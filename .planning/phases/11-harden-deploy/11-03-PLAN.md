---
phase: 11-harden-deploy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/hooks/useMatchDetail.ts
  - client/src/hooks/useMatchDetail.test.ts
  - client/src/hooks/useDraftDetail.test.ts
  - client/src/hooks/useMatchIntel.test.ts
  - client/src/hooks/useWinProbability.test.ts
autonomous: true
requirements: []   # hardening — no direct REQ mapping; acceptance bar = ROADMAP criterion 3
user_setup: []

must_haves:
  truths:
    - "When game_state === 6 (post-game), every poller's refetchInterval evaluates to false so finished matches stop draining upstream quota"
    - "Each polling-cadence helper has an explicit unit test asserting the game_state===6 → false case"
  artifacts:
    - path: "client/src/hooks/useMatchDetail.ts"
      provides: "Pure computeMatchInterval(gameState) helper extracted from the inline refetchInterval ternary"
      exports: ["computeMatchInterval"]
    - path: "client/src/hooks/useMatchDetail.test.ts"
      provides: "Unit test asserting computeMatchInterval(6) === false (and 30_000 for in-game)"
      min_lines: 15
  key_links:
    - from: "client/src/hooks/useMatchDetail.ts"
      to: "useQuery refetchInterval"
      via: "refetchInterval: computeMatchInterval(matchFromCache?.game_state)"
      pattern: "computeMatchInterval"
---

<objective>
Verify and lock with tests that all four match-page pollers stop (`refetchInterval === false`) once `game_state === 6`. Satisfies ROADMAP criterion 3 (D-11).

Purpose: Finished matches must stop draining upstream quota. The cadence helpers already encode the guard; this plan extracts the one inline case into a pure helper and adds explicit `===6 → false` assertions across all four, so a future edit can't silently reintroduce post-game polling.

Output: a pure `computeMatchInterval` helper + its test; explicit `game_state===6` assertions added to the draft/intel/winprob helper tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-harden-deploy/11-RESEARCH.md
@.planning/phases/11-harden-deploy/11-PATTERNS.md
@.planning/phases/11-harden-deploy/11-VALIDATION.md

<interfaces>
<!-- The three pure helpers already encode the guard (11-PATTERNS.md lines 358-367): -->
<!-- computeWinProbInterval (useWinProbability.ts:25) — `if (gameState===6) return false` is the FIRST guard; test line 11 already asserts it -->
<!-- computeDraftInterval (useDraftDetail.ts:48-51) — `gameState===2 → 5000; else false` (covers 6) — add explicit ===6 assertion -->
<!-- computeIntelInterval (useMatchIntel.ts:33-36) — same shape — add explicit ===6 assertion -->

<!-- useMatchDetail.ts:39 — INLINE ternary, no pure helper yet: -->
```ts
refetchInterval: matchFromCache?.game_state === 6 ? false : 30_000,
```
<!-- Extract to a pure exported computeMatchInterval(gameState) mirroring the other three helpers. -->

<!-- Test convention to replicate: useWinProbability.test.ts lines 10-12 (the "===6 first guard" assertion). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract computeMatchInterval pure helper + add its ===6 test</name>
  <read_first>
    - client/src/hooks/useMatchDetail.ts (FULL FILE — line 38-40 inline refetchInterval ternary to extract; preserve D-12/D-14 comments)
    - client/src/hooks/useWinProbability.ts (lines 20-26 — the pure-helper shape + doc-comment to mirror for computeMatchInterval)
    - client/src/hooks/useWinProbability.test.ts (lines 1-12 — the "===6 → false first guard" test template to copy)
    - 11-PATTERNS.md §"Polling-stop verification" (lines 356-367) and §"Pure-helper + unit-test split" (lines 389-391)
  </read_first>
  <action>
    In `client/src/hooks/useMatchDetail.ts`, extract the inline `refetchInterval` ternary (line 39) into a pure exported helper mirroring `computeWinProbInterval` (same doc-comment cadence-contract style):
    ```ts
    /**
     * Polling cadence for the in-game match detail poller (D-12 / D-14).
     *   game_state === 6 (post-game) → false  (CLAUDE.md §Critical Pitfalls — MUST stop; finished matches drain quota)
     *   anything else                → 30_000 ms (30s in-game cadence)
     */
    export function computeMatchInterval(gameState: number | undefined): number | false {
      return gameState === 6 ? false : 30_000
    }
    ```
    Replace the inline use at line 39 with `refetchInterval: computeMatchInterval(matchFromCache?.game_state),`. Keep all other query config unchanged (queryKey, queryFn, staleTime). Behavior is identical — this is a pure extraction so the case is testable without React.

    Create `client/src/hooks/useMatchDetail.test.ts` (new) following the useWinProbability.test.ts template. Import `{ computeMatchInterval }`. Assert:
    - `computeMatchInterval(6)` === `false` (post-game stops — criterion 3)
    - `computeMatchInterval(5)` === `30_000` (in-game polls)
    - `computeMatchInterval(undefined)` === `30_000`
  </action>
  <verify>
    <automated>cd client && npx vitest run src/hooks/useMatchDetail.test.ts</automated>
  </verify>
  <done>computeMatchInterval is exported and used as the refetchInterval; its test asserts 6→false and 5/undefined→30_000; test exits 0; useMatchDetail behavior unchanged.</done>
  <acceptance_criteria>
    - `useMatchDetail.ts exports computeMatchInterval and uses it for refetchInterval` — grep `export function computeMatchInterval` and `refetchInterval: computeMatchInterval` in useMatchDetail.ts
    - `useMatchDetail.test.ts asserts computeMatchInterval(6) === false` — grep `computeMatchInterval(6)` and `false` in the test
    - `cd client && npx vitest run src/hooks/useMatchDetail.test.ts exits 0` (VALIDATION criterion 3)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add explicit game_state===6 assertions to draft/intel/winprob helper tests</name>
  <read_first>
    - client/src/hooks/useDraftDetail.test.ts (existing cadence tests — add an explicit ===6 case)
    - client/src/hooks/useMatchIntel.test.ts (existing cadence tests — add an explicit ===6 case)
    - client/src/hooks/useWinProbability.test.ts (lines 10-12 — confirm the ===6 assertion already exists; if not, add it)
    - 11-PATTERNS.md §"Polling-stop verification" (lines 356-367 — the helper guard map)
  </read_first>
  <action>
    Add (or confirm) an explicit `game_state === 6 → false` assertion in each pure-helper test, so all four pollers are covered by name:
    - `useDraftDetail.test.ts`: add `expect(computeDraftInterval(6)).toBe(false)` (post-game stops).
    - `useMatchIntel.test.ts`: add `expect(computeIntelInterval(6)).toBe(false)`.
    - `useWinProbability.test.ts`: confirm `expect(computeWinProbInterval(6, …)).toBe(false)` exists (PATTERNS says test line 11 already asserts it); if present, leave it — if the signature needs a 2nd arg, pass the existing test's representative value. Do NOT change the helper implementations; this task only strengthens test coverage.
    Place each new assertion inside the existing `describe` block for that helper, next to the current cadence assertions (do not create new files).
  </action>
  <verify>
    <automated>cd client && npx vitest run src/hooks</automated>
  </verify>
  <done>computeDraftInterval(6), computeIntelInterval(6), and computeWinProbInterval(6,…) each have an explicit ===false assertion; all hook tests green.</done>
  <acceptance_criteria>
    - `useDraftDetail.test.ts asserts computeDraftInterval(6) === false` — grep `computeDraftInterval(6)` in the test
    - `useMatchIntel.test.ts asserts computeIntelInterval(6) === false` — grep `computeIntelInterval(6)` in the test
    - `useWinProbability.test.ts asserts the ===6 false case` — grep `computeWinProbInterval(6` in the test
    - `cd client && npx vitest run src/hooks exits 0` (VALIDATION criterion 3, all pollers)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client poller → BFF → upstream | Each client refetch ultimately triggers (at most one per TTL) an upstream call; a poller that never stops indirectly drains quota for a finished match. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Denial of Service | post-game match continuing to poll and drain upstream quota (refetchInterval helpers) | mitigate | All four cadence helpers return `false` at `game_state===6`; pure-helper unit tests lock the guard against silent regression (criterion 3, D-11). Defense-in-depth with Plan 01's server-side queue (T-11-01). |
</threat_model>

<verification>
- `cd client && npx vitest run src/hooks` green (all four cadence helpers assert ===6 → false).
- `cd client && npm run build` green (tsc + vite) — confirms the computeMatchInterval extraction compiles.
</verification>

<success_criteria>
- ROADMAP criterion 3 met: polling stops automatically (`refetchInterval === false`) once `game_state === 6`, verified by explicit unit tests across all four pollers (D-11).
- No behavior change — pure extraction + added assertions only.
</success_criteria>

<output>
After completion, create `.planning/phases/11-harden-deploy/11-03-SUMMARY.md`
</output>
