---
phase: 11-harden-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/src/components/BentoErrorBoundary.tsx
  - client/src/components/BentoErrorBoundary.test.tsx
  - client/src/pages/MatchPage.tsx
  - client/src/App.tsx
autonomous: true
requirements: []   # hardening — no direct REQ mapping; acceptance bar = ROADMAP criterion 1
user_setup: []

must_haves:
  truths:
    - "A render crash in one Match panel (e.g. IntelTooltip indexing a malformed payload) renders a bento-styled 'couldn't load' fallback in that panel only — sibling panels keep rendering"
    - "The fallback shows a generic message and a Retry button; clicking Retry re-mounts the panel's children"
    - "The fallback never renders a stack trace to the UI; the stack goes to console only"
    - "Each route (Home, Match) is wrapped in a top-level boundary as a backstop"
  artifacts:
    - path: "client/src/components/BentoErrorBoundary.tsx"
      provides: "Reusable per-card error boundary with bento-styled fallback + Retry"
      exports: ["BentoErrorBoundary"]
    - path: "client/src/components/BentoErrorBoundary.test.tsx"
      provides: "RTL tests: fallback render, sibling isolation, Retry re-mount"
      min_lines: 30
  key_links:
    - from: "client/src/pages/MatchPage.tsx"
      to: "client/src/components/BentoErrorBoundary.tsx"
      via: "each bento-card child wrapped in <BentoErrorBoundary resetKeys={[matchId]}>"
      pattern: "BentoErrorBoundary"
    - from: "client/src/App.tsx"
      to: "client/src/components/BentoErrorBoundary.tsx"
      via: "each route element wrapped"
      pattern: "BentoErrorBoundary"
---

<objective>
Add per-bento-card error boundaries (primary isolation) plus route-level boundaries (backstop) so one failing widget never blanks the match screen. Satisfies ROADMAP criterion 1.

Purpose: During a full day of viewing, a malformed upstream payload that makes one widget throw during render must isolate to that card with a recoverable fallback — not blank the whole match page. Reuses the project's `.bento-card` surface so a failed widget still looks intentional.

Output: `BentoErrorBoundary.tsx` (new, react-error-boundary v6) + tests, every `.bento-card` child in MatchPage wrapped, both routes wrapped in App.tsx, dep bumped `^4 → ^6`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-harden-deploy/11-RESEARCH.md
@.planning/phases/11-harden-deploy/11-PATTERNS.md
@.planning/phases/11-harden-deploy/11-VALIDATION.md

<interfaces>
<!-- react-error-boundary v6 surface (RESEARCH Pattern 3, lines 223-256). Client currently pins ^4 — MUST bump to ^6. -->
<!-- v5+ renamed useErrorHandler → useErrorBoundary; FallbackProps = { error, resetErrorBoundary }. -->
```tsx
import { ErrorBoundary, type FallbackProps } from 'react-error-boundary'
// <ErrorBoundary FallbackComponent={Fallback} resetKeys={[...]} onError={(error, info) => ...}>
```

<!-- .bento-card surface to reuse (client/src/index.css:57-63) — same class so the fallback looks intentional. -->
<!-- --color-surface, 1px --color-border, --radius-lg. Tokens: text-text, text-text-dim, hover:text-primary. -->

<!-- Match bento-card blocks to wrap (client/src/pages/MatchPage.tsx) — confirmed via grep, children get wrapped: -->
<!-- :67 winprob, :96 draft section, :119 heroes, :127 items, :135 cooldowns, :155 Row2-left cluster, :161 Row2-right -->
<!-- matchId is in scope (MatchPage useParams). Pass resetKeys={[matchId]}. -->

<!-- App.tsx routes (lines 5-12): wrap each element in <BentoErrorBoundary>. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump dep to ^6, build BentoErrorBoundary.tsx + test (Wave 0 test first)</name>
  <read_first>
    - client/package.json (line 19 — react-error-boundary pinned `^4.0.0` → bump to `^6`)
    - client/src/index.css (lines 57-69 — `.bento-card` surface to reuse)
    - client/src/components/ErrorBanner.tsx (existing "Couldn't load…" copy/tone to mirror, but render inside .bento-card, NOT the red danger banner)
    - client/src/components/RoshanBlock.test.tsx (lines 1-3, 9-16 — the canonical RTL test convention: render/screen/act, afterEach real timers)
    - client/src/pages/MatchPage.tsx (lines 47-49 — the back-nav button idiom `text-[11px] uppercase tracking-[…] text-text-dim hover:text-primary` to copy for the Retry button)
    - 11-PATTERNS.md §"BentoErrorBoundary.tsx" (lines 232-279) and §"BentoErrorBoundary.test.tsx" (lines 282-291)
    - 11-RESEARCH.md Pattern 3 (lines 219-258), Pitfall 3 v4→v6 drift (lines 319-322), Pitfall 4 boundaries-catch-render-only (lines 324-327)
  </read_first>
  <behavior>
    - Test 1 (criterion 1): a child component that throws during render (a `<Thrower/>` whose body does `throw new Error('boom')`) causes BentoErrorBoundary to render the fallback — assert `screen.getByText(/couldn't load/i)` and `screen.getByRole('button', { name: /retry/i })` are present.
    - Test 2 (sibling isolation): render two siblings, one wrapped boundary around `<Thrower/>` and one wrapped boundary around a normal `<div>OK</div>`; assert the fallback shows AND `screen.getByText('OK')` still renders.
    - Test 3 (criterion 1b, Retry re-mount): a Thrower that throws only on first render then renders 'recovered'; click Retry; assert children re-mount and 'recovered' appears (or simply that the fallback clears). Suppress the expected React error log noise as RoshanBlock-style tests do.
    - Test 4 (security): the fallback does NOT render the error message/stack text — assert the thrown message 'boom' is NOT in the document (generic copy only).
  </behavior>
  <action>
    Bump the dep: in `client/package.json` line 19 change `"react-error-boundary": "^4.0.0"` to `"react-error-boundary": "^6"`, then run `npm install` inside `client/` to resolve v6.1.x (React-19-safe; peerDeps react ^18||^19).

    Create `client/src/components/BentoErrorBoundary.tsx` exactly per RESEARCH Pattern 3 (v6 API: FallbackComponent + resetErrorBoundary + resetKeys + onError). The fallback renders inside `.bento-card`, shows a GENERIC message (never the stack — SECURITY T-11-05), and a Retry button using the back-nav idiom:
    ```tsx
    import { ErrorBoundary, type FallbackProps } from 'react-error-boundary'
    import type { ReactNode } from 'react'

    function BentoFallback({ resetErrorBoundary }: FallbackProps) {
      return (
        <div className="bento-card flex flex-col items-center justify-center gap-2 text-center">
          <span aria-hidden className="text-text-dim">⚠</span>
          <p className="text-sm text-text">Couldn't load this panel.</p>
          <button
            onClick={resetErrorBoundary}
            className="text-[11px] uppercase tracking-[0.2em] text-text-dim hover:text-primary"
          >Retry</button>
        </div>
      )
    }

    export function BentoErrorBoundary({ children, resetKeys }: { children: ReactNode; resetKeys?: unknown[] }) {
      return (
        <ErrorBoundary
          FallbackComponent={BentoFallback}
          resetKeys={resetKeys}
          onError={(error, info) => console.error('[bento-boundary]', error, info.componentStack)}
        >
          {children}
        </ErrorBoundary>
      )
    }
    ```
    Note: do NOT destructure or render `error` in BentoFallback — the stack must never reach the UI (SECURITY). It is logged via `onError` to console only.

    Create `client/src/components/BentoErrorBoundary.test.tsx` mirroring RoshanBlock.test.tsx RTL conventions (render/screen/act, afterEach real timers). Implement the four behavior tests. Temporarily silence the expected `console.error` from React's boundary logging during the throwing tests (spy + restore).
  </action>
  <verify>
    <automated>cd client && npx vitest run src/components/BentoErrorBoundary.test.tsx</automated>
  </verify>
  <done>react-error-boundary is `^6` in client/package.json; BentoErrorBoundary renders a bento-styled fallback with a Retry button on a render crash, isolates siblings, re-mounts on Retry, and never renders the stack; test exits 0.</done>
  <acceptance_criteria>
    - `client/package.json pins react-error-boundary ^6` — grep `"react-error-boundary": "^6"` in client/package.json
    - `BentoErrorBoundary.tsx renders a Retry button and uses .bento-card` — grep `bento-card`, `Retry`, `FallbackComponent` in the component
    - `BentoFallback does not render the error/stack` — confirm `error` is not interpolated into JSX in BentoErrorBoundary.tsx
    - `cd client && npx vitest run src/components/BentoErrorBoundary.test.tsx exits 0` (VALIDATION criteria 1/1b)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wrap every MatchPage bento-card child + both App routes</name>
  <read_first>
    - client/src/pages/MatchPage.tsx (FULL FILE — the bento-card wrapper blocks at lines 67, 96, 119, 127, 135, 155, 161; matchId from useParams in scope)
    - client/src/App.tsx (FULL FILE — lines 5-12, Routes/Route structure)
    - 11-PATTERNS.md §"MatchPage.tsx" (lines 295-305) and §"App.tsx" (lines 309-315)
  </read_first>
  <action>
    In `client/src/pages/MatchPage.tsx`: import `{ BentoErrorBoundary } from '../components/BentoErrorBoundary'`. For EVERY `<div className="bento-card …">…</div>` block (grep `bento-card` to enumerate them — currently 7: lines 67, 96, 119, 127, 135, 155, 161), wrap the block's CHILDREN in `<BentoErrorBoundary resetKeys={[matchId]}>…</BentoErrorBoundary>`. Keep the outer `<div className="bento-card …">` wrapper and its layout classes unchanged — only the inner children are wrapped, so the boundary's fallback inherits the card frame and a stuck boundary auto-clears when `matchId` changes (RESEARCH line 255). Do not change layout/order of any block.

    In `client/src/App.tsx`: import `{ BentoErrorBoundary }` and wrap each route element as a backstop (D-07):
    ```tsx
    <Route path="/" element={<BentoErrorBoundary><HomePage /></BentoErrorBoundary>} />
    <Route path="/match/:matchId" element={<BentoErrorBoundary><MatchPage /></BentoErrorBoundary>} />
    ```
  </action>
  <verify>
    <automated>cd client && npm run build</automated>
  </verify>
  <done>Every MatchPage bento-card has its children wrapped in a per-card boundary keyed by matchId; both routes wrapped as backstop; tsc + vite build green; full client suite still green.</done>
  <acceptance_criteria>
    - `MatchPage wraps cards in BentoErrorBoundary with resetKeys={[matchId]}` — grep count of `BentoErrorBoundary` in MatchPage.tsx is ≥ 7 and `resetKeys={[matchId]}` present
    - `App.tsx wraps both routes` — grep `BentoErrorBoundary` appears wrapping both `<HomePage` and `<MatchPage` in App.tsx
    - `cd client && npm run build exits 0` (tsc + vite build)
    - `cd client && npm test -- --run exits 0` (full client suite, per-wave merge gate — confirms no existing test regressed)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream payload → React render | Malformed/unexpected API data reaches component render; a widget may throw while indexing into it. |
| Caught error → UI fallback | The error object (with stack) must not cross into rendered UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Information Disclosure | error-boundary leaking stack traces to UI (BentoFallback) | mitigate | BentoFallback renders a generic "Couldn't load this panel." message only; `error`/stack never interpolated into JSX; stack logged via `onError` to console only (RESEARCH §Security line 467). Test 4 asserts the thrown message is absent from the DOM. |
| T-11-06 | Denial of Service (client-side blanking) | one widget crash blanking the whole match screen | mitigate | Per-bento-card boundaries isolate render crashes to a single card; route-level boundary is a backstop only; sibling-isolation test proves containment (criterion 1) |
</threat_model>

<verification>
- `cd client && npx vitest run src/components/BentoErrorBoundary.test.tsx` green (fallback, isolation, retry, no-stack).
- `cd client && npm run build` green (tsc + vite) — confirms v6 import shape compiles.
- `cd client && npm test -- --run` green — no existing component test regressed by the wrapping.
</verification>

<success_criteria>
- ROADMAP criterion 1 met: every route-level component is wrapped in an error boundary so one failing widget does not blank the match screen.
- D-05 (per-bento-card granularity), D-06 (mini bento fallback + Retry re-mount), D-07 (route-level backstop) implemented.
- react-error-boundary bumped `^4 → ^6` (Pitfall 3 resolved).
</success_criteria>

<output>
After completion, create `.planning/phases/11-harden-deploy/11-02-SUMMARY.md`
</output>
