---
phase: 10-historical-graphs
plan: 04
type: execute
wave: 3
depends_on: [10-02, 10-03]
files_modified:
  - client/src/hooks/useMatchDetail.ts
  - client/src/pages/MatchPage.tsx
autonomous: false
requirements: []
tags: [client, mount, hook, dota, layout-preservation]

must_haves:
  truths:
    - "useMatchDetail return surface includes `history: HistorySample[]` (always an array, defaults to []) — type widens via the EnrichedLiveGameSchema change from Plan 02"
    - "MatchPage mounts <HistoryGraphs> in a single new <section> placed below ScoreHeader and above the player rows region"
    - "No existing layout siblings on MatchPage are reordered, removed, restyled, or re-flowed"
    - "<HistoryGraphs> receives history, gameDuration, and gameState from useMatchDetail return values"
    - "When game_state === 6 and Redis has cleaned up, history is [] and the section degrades to skeleton (D-25)"
  artifacts:
    - path: "client/src/hooks/useMatchDetail.ts"
      provides: "Return surface widened to include `history`"
      exports: ["default"]
      contains: "history"
    - path: "client/src/pages/MatchPage.tsx"
      provides: "<HistoryGraphs> mounted in a single dedicated section"
      contains: "HistoryGraphs"
  key_links:
    - from: "client/src/pages/MatchPage.tsx"
      to: "client/src/components/HistoryGraphs.tsx"
      via: "default import"
      pattern: "import HistoryGraphs from"
    - from: "client/src/pages/MatchPage.tsx"
      to: "client/src/hooks/useMatchDetail.ts"
      via: "destructure history from hook"
      pattern: "history.*=.*useMatchDetail|history\\}\\s*=\\s*useMatchDetail|useMatchDetail.*history"
---

<objective>
Final wiring for Phase 10: surface `history` through `useMatchDetail` and mount `<HistoryGraphs>` on the match page in a single, dedicated, NON-disruptive section.

Purpose: Connect the server-side time-series (Plans 01-02) to the client component (Plan 03). This plan is layout-sensitive — the user's auto-memory `feedback_layout_preservation` requires that we DO NOT silently restructure the existing match page. The mount point is the critical decision; everything else is mechanical.

Output: One hook line change + one MatchPage section addition. Plan is `autonomous: false` — execution pauses at the mount checkpoint for user confirmation of placement.
</objective>

<execution_context>
@D:/MateProjects/projects/dota/dota_stats/.claude/get-shit-done/workflows/execute-plan.md
@D:/MateProjects/projects/dota/dota_stats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-historical-graphs/10-CONTEXT.md
@.planning/phases/10-historical-graphs/10-PATTERNS.md
@.planning/phases/10-historical-graphs/10-02-SUMMARY.md
@.planning/phases/10-historical-graphs/10-03-SUMMARY.md
@client/src/hooks/useMatchDetail.ts
@client/src/pages/MatchPage.tsx
@client/src/components/HistoryGraphs.tsx
@client/src/components/RoshanBlock.tsx
@client/src/components/WinProbBar.tsx

<interfaces>
<!-- Hook contract (current) — match: EnrichedLiveGame from server bff schema. -->
<!-- After Plan 02, EnrichedLiveGame has `history: HistorySample[]` automatically. -->

From client/src/hooks/useMatchDetail.ts (current shape, around lines 61-69):
```typescript
return {
  match,
  radiantPlayers,
  direPlayers,
  buildings,
  isLoading: query.isLoading,
  gameState: match?.game_state,
}
```

From client/src/components/HistoryGraphs.tsx (Plan 03 default export):
```typescript
export interface HistoryGraphsProps {
  history: Array<{ t: number; gold: number; xp: number }>
  gameDuration: number | undefined
  gameState: number | undefined
}
export default function HistoryGraphs(props: HistoryGraphsProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Surface history through useMatchDetail return value</name>
  <files>client/src/hooks/useMatchDetail.ts</files>
  <read_first>
    - client/src/hooks/useMatchDetail.ts (current state — locate the `return { match, radiantPlayers, ... }` literal around lines 61-69)
    - .planning/phases/10-historical-graphs/10-PATTERNS.md §"client/src/hooks/useMatchDetail.ts"
  </read_first>
  <action>
Edit `client/src/hooks/useMatchDetail.ts`. Add a single line to the return object:

```typescript
return {
  match,
  radiantPlayers,
  direPlayers,
  buildings,
  history: match?.history ?? [],          // NEW — always an array (D-20, D-25)
  isLoading: query.isLoading,
  gameState: match?.game_state,
}
```

DO NOT add any new query hook. DO NOT change `useQuery` config. DO NOT add any new fetch. The `history` field arrives through the existing `['live-games']` cache via the schema change in Plan 02.

The `?? []` fallback handles three cases simultaneously:
- match is undefined while loading
- post-match cleanup ran on the server (D-25 degrades to skeleton on the client)
- legacy cached payload from before this deploy
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit -p tsconfig.app.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "history:\s*match\?\.history\s*\?\?\s*\[\]" client/src/hooks/useMatchDetail.ts` matches.
    - Existing return fields (match, radiantPlayers, direPlayers, buildings, isLoading, gameState) are still present (`grep -n "radiantPlayers" client/src/hooks/useMatchDetail.ts` and similar checks).
    - No new `useQuery` call is added (`grep -c "useQuery" client/src/hooks/useMatchDetail.ts` count is unchanged from before this edit).
    - `cd client && npx tsc --noEmit -p tsconfig.app.json` exits 0.
  </acceptance_criteria>
  <done>Hook return surface widened by exactly one line; type flows from EnrichedLiveGameSchema.history; no new fetch logic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm HistoryGraphs mount placement on MatchPage (LAYOUT memory)</name>
  <what-built>
    Plans 01-03 are complete: server samples to Redis, BFF emits `history` on every `/api/live/games` enriched game, the `<HistoryGraphs>` component renders skeleton or dual SVG charts depending on `history.length`. NOTHING has been mounted to the page yet.
  </what-built>
  <how-to-verify>
    The user's auto-memory `feedback_layout_preservation` REQUIRES explicit confirmation before re-flowing the match page. Read `client/src/pages/MatchPage.tsx` end-to-end. Identify the proposed mount slot:

    **Proposed slot:** A new `<section>` placed BETWEEN the ScoreHeader region (and any sibling like WinProbBar) and the player rows / scoreboard region. This is the same slot recommended by CONTEXT `code_context` ("below score header / above player rows"). It mirrors how RoshanBlock and WinProbBar are mounted as siblings.

    Present the user with:
    1. The exact line range where ScoreHeader/WinProbBar currently end.
    2. The exact line where player rows begin.
    3. A short before/after snippet showing the new `<section><HistoryGraphs ... /></section>` insertion (NO restyling of siblings, NO re-flow).
    4. The wrapper `<section>` should have `style={{ marginTop: 16 }}` (or the existing inter-section spacing convention used elsewhere on this page) and NO position:relative override (HistoryGraphs creates its own position:relative wrapper internally per Plan 03).

    Wait for the user to either:
    - Approve the proposed slot ("approved" / "yes" / "ok").
    - Specify a different placement (e.g., "place it below player rows", "place it inside the WinProbBar section", etc).

    If the user picks an alternative, capture the exact line/anchor element they specify before proceeding to Task 3.
  </how-to-verify>
  <resume-signal>Type "approved" to use the proposed slot, or describe an alternative placement.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mount <HistoryGraphs> on MatchPage at the approved slot</name>
  <files>client/src/pages/MatchPage.tsx</files>
  <read_first>
    - client/src/pages/MatchPage.tsx (current state — locate the slot approved in Task 2)
    - client/src/components/HistoryGraphs.tsx (default export shape)
    - client/src/hooks/useMatchDetail.ts (Task 1 — destructure `history` from hook)
    - .planning/phases/10-historical-graphs/10-PATTERNS.md §"client/src/pages/MatchPage.tsx" (mount snippet)
  </read_first>
  <action>
Edit `client/src/pages/MatchPage.tsx`.

**Step 1 — Add import** at the top, with the other component imports:
```typescript
import HistoryGraphs from '../components/HistoryGraphs'
```

**Step 2 — Destructure `history`** from the existing `useMatchDetail()` call. Wherever the page already destructures `match, radiantPlayers, direPlayers, buildings, gameState, isLoading`, ADD `history` to that destructure:
```typescript
const { match, radiantPlayers, direPlayers, buildings, history, gameState, isLoading } = useMatchDetail(matchId)
```

**Step 3 — Insert the mount** at the slot approved in Task 2 (default: between ScoreHeader-region and player rows). Use this snippet verbatim:
```tsx
{/* Phase 10: historical graphs — self-gates internally (skeleton when history.length < 2) */}
<section style={{ marginTop: 16 }}>
  <HistoryGraphs
    history={history}
    gameDuration={match?.duration}
    gameState={match?.game_state}
  />
</section>
```

**Hard rules — do NOT violate (layout-preservation memory):**
- Do NOT modify any existing JSX element or its props.
- Do NOT change any existing wrapper's `style`, `className`, ordering, or nesting.
- Do NOT introduce a new flex/grid container around existing siblings.
- Do NOT remove or rename any existing prop on RoshanBlock, WinProbBar, ScoreHeader, or scoreboard rows.
- The new `<section>` is a SIBLING of the existing sections, inserted at the approved index.

If the user specified a non-default placement in Task 2, follow that placement exactly; otherwise use the default ("below ScoreHeader region, above player rows").
  </action>
  <verify>
    <automated>cd client && npx tsc --noEmit -p tsconfig.app.json && cd client && npx vitest run src/pages</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import HistoryGraphs from" client/src/pages/MatchPage.tsx` matches.
    - `grep -nE "<HistoryGraphs[^>]*history=\{history\}" client/src/pages/MatchPage.tsx` matches.
    - `grep -nE "gameDuration=\{match\?\.duration\}" client/src/pages/MatchPage.tsx` matches.
    - `grep -nE "gameState=\{match\?\.game_state\}" client/src/pages/MatchPage.tsx` matches.
    - `grep -nE "history,\s*gameState|history\s+\}\s*=\s*useMatchDetail" client/src/pages/MatchPage.tsx` matches (destructure includes history).
    - `cd client && npx tsc --noEmit -p tsconfig.app.json` exits 0.
    - Existing match-page test suite (if any) passes — `cd client && npx vitest run src/pages` exits 0.
    - Manual diff review: `git diff client/src/pages/MatchPage.tsx` shows ONLY (a) one new import line, (b) `history` added to a destructure, and (c) one new `<section><HistoryGraphs … /></section>` block. No other lines should differ.
  </acceptance_criteria>
  <done>HistoryGraphs is mounted at the user-approved slot; existing layout siblings are byte-identical except for the additive section; type-check and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook return → page consumers | `history` flows from server-validated schema; consumers treat it as `HistorySample[]` (read-only render) |
| Page mount → existing UI | New section is a sibling-only insertion; no shared mutable state with existing components |

## STRIDE Threat Register

| Threat ID | Category | Component | Description | Severity | Mitigation Plan | Residual Risk |
|-----------|----------|-----------|-------------|----------|-----------------|---------------|
| T-10-16 | Tampering | Page layout regression | Re-flowing the match page would silently break user's mental model and contradict `feedback_layout_preservation` memory | medium | `autonomous: false` checkpoint (Task 2) gates the mount; Task 3 hard rules forbid touching any existing element; acceptance grep enforces ONLY-additive diff | mitigate (human verification on the placement choice itself) |
| T-10-17 | Denial of Service | History rendering during loading | If `match` is undefined during initial fetch, accessing `match.history` would throw | low | `match?.history ?? []` fallback in hook (Task 1) ensures `history` is always an array; HistoryGraphs self-gates on length < 2 | mitigate |
| T-10-18 | Information Disclosure | Stale post-match history shown to user (D-25) | After server DEL/TTL, client cache may still hold the last response with non-empty history | low | D-25 explicitly accepts this: "Charts continue to display final history after game_state === 6 while the user remains on the page". On reload, the section gracefully shows skeleton (D-25 verified by Plan 03 self-gating) | accept (intended behaviour per CONTEXT) |
</threat_model>

<verification>
- `cd client && npx tsc --noEmit -p tsconfig.app.json` exits 0.
- `cd client && npx vitest run` exits 0 (full client suite green — no regression).
- `git diff client/src/pages/MatchPage.tsx | grep -E "^[-+]" | grep -vE "^(---|\+\+\+|.*HistoryGraphs.*|.*history,.*|.*<section style=\{\{ marginTop:.*|.*</section>|.*phase 10.*|.*Phase 10.*|^\+\s*$|^-\s*$)"` returns ZERO lines (only additive HistoryGraphs-related lines changed).
</verification>

<success_criteria>
- Hook surfaces `history: HistorySample[]` to consumers.
- HistoryGraphs is mounted on MatchPage at the user-approved slot.
- No existing match-page layout sibling is restructured.
- Full client test suite green.
- D-25 graceful degradation works: post-match navigate-then-reload shows skeleton, not stale chart.
</success_criteria>

<output>
After completion, create `.planning/phases/10-historical-graphs/10-04-SUMMARY.md` documenting: hook return shape, exact mount slot (file/line), confirmed placement decision (default or user-overridden), regression test status.
</output>
