---
phase: 10.4
slug: visual-redesign-and-responsive
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-06
---

# Phase 10.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Note: This is a restyle + responsive phase. Most correctness is **visual** and irreducibly
> manual (jsdom has no layout engine). Automation focuses on regression-prevention:
> token presence, no-remaining-hardcoded-hex, and keeping the existing 105 client tests green.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 2.x + @testing-library/react + jsdom |
| **Config file** | client/ (vitest via package.json `test` script) |
| **Quick run command** | `npm run test --prefix client` |
| **Full suite command** | `npm run test --prefix client && npm run build --prefix client` |
| **Estimated runtime** | ~15-30 seconds (tests) + build |

---

## Sampling Rate

- **After every task commit:** Run `npm run test --prefix client`
- **After every plan wave:** Run full suite (`test` + `tsc`/`build`)
- **Before `/gsd-verify-work`:** Full suite green + manual UAT at all 4 widths complete
- **Max feedback latency:** ~30 seconds (automated); manual UAT at phase gate

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 10.4-00-01 | 00 | 0 | UI-01 | — | N/A | unit-fix | `npm run test --prefix client -- HistoryGraphs` | ✅ exists | ⬜ pending |
| 10.4-00-02 | 00 | 0 | UI-01 | — | N/A | grep | `grep -c "@theme" client/src/index.css` | ❌ W0 | ⬜ pending |
| 10.4-*-* | * | 1+ | UI-01 | — | N/A | regression | `npm run test --prefix client` (105 green) | ✅ exists | ⬜ pending |
| 10.4-*-* | * | 1+ | UI-01 | — | N/A | grep | no remaining `#0a0a0a`/`#4ade80`/`#ef4444`/`#d8d8d8` literals in migrated files | ✅ exists | ⬜ pending |
| 10.4-*-* | * | 2+ | UI-02, UI-03 | — | N/A | manual | reflow at 375/768/1280/1536 (see Manual-Only) | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `client/src/components/HistoryGraphs.test.tsx` — update the 6 assertions that hard-code `#4ade80` / `#ef4444` / `style.color === '#4ade80'` to the new softened team tokens (or assert `var(--…)` strings) BEFORE the component migrates — otherwise success criterion 5 (suite green) fails.
- [ ] `client/src/index.css` — establish the full Tactical Slate `@theme` token layer (currently only ~2 of ~30 tokens present); copy values from `.planning/sketches/themes/tactical-slate.css` (LOCKED — copy, don't import).
- [ ] Font loading (Inter + JetBrains Mono) wired without FOUT / build break.

*Existing vitest infrastructure covers regression; Wave 0 adds the token layer + fixes the one breaking test.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Home matches sketch 003-B visually | UI-01 | "matches sketch" is a visual judgment | Run app, open Home, compare against `.planning/sketches/003-home-redesign/index.html` (theme=tactical-slate) |
| Match page matches sketch 004-B visually | UI-01 | visual judgment; real data | Open a live match, compare against `.planning/sketches/004-match-page-tactical-slate/index.html` |
| No horizontal scroll / overlap / clipping at phone 375px | UI-02, UI-03 | jsdom has no layout engine | DevTools responsive mode @375px on Home + Match — verify reflow, no overflow |
| Reflow at tablet 768px | UI-02 | layout engine needed | DevTools @768px — Match 3-col→2-col, map reflow |
| Reflow at laptop 1280px | UI-02 | layout engine needed | DevTools @1280px — full multi-col layout intact |
| Reflow at desktop 1536px+ | UI-02 | layout engine needed | DevTools @1536px — no excessive whitespace, max-width respected |
| Dense data legible on phone (portraits, 6 item slots, cooldown icons) | UI-03 | visual legibility judgment | @375px verify hero portraits / item slots / cooldown icons remain readable per UI-SPEC minimum sizes |

---

## Validation Sign-Off

- [ ] All tasks have automated verify (grep/test) OR are listed in Manual-Only with instructions
- [ ] Sampling continuity: regression test runs after every wave
- [ ] Wave 0 covers the breaking HistoryGraphs test + token layer + fonts
- [ ] No watch-mode flags (vitest run, not watch)
- [ ] Feedback latency < 30s for automated checks
- [ ] `nyquist_compliant: true` set in frontmatter (after planner maps every task)

**Approval:** pending
