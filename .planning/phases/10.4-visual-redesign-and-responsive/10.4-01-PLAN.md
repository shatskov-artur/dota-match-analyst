---
phase: 10.4-visual-redesign-and-responsive
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - client/src/index.css
  - client/index.html
  - client/src/components/HistoryGraphs.test.tsx
  - client/src/components/HistoryGraphs.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "The full Tactical Slate palette is declared once as Tailwind 4 @theme tokens in index.css"
    - "Every token name from tactical-slate.css resolves at runtime as a var(--...) custom property"
    - "Inter + JetBrains Mono load via Google Fonts <link> with system fallbacks (no FOUT/build break)"
    - "The 6 HistoryGraphs.test.tsx assertions target the softened team colors so the suite stays green after HistoryGraphs migrates"
  artifacts:
    - path: "client/src/index.css"
      provides: "Full @theme token layer + :root var()-only tokens (gradients/glows/soft tints)"
      contains: "@theme"
    - path: "client/index.html"
      provides: "Google Fonts preconnect + Inter/JetBrains Mono stylesheet link"
      contains: "fonts.googleapis.com"
    - path: "client/src/components/HistoryGraphs.test.tsx"
      provides: "Updated assertions for softened radiant/dire colors (#6bcf8a / #e06a72)"
      contains: "#6bcf8a"
  key_links:
    - from: "client/src/index.css @theme"
      to: "runtime :root custom properties"
      via: "Tailwind 4 @theme auto-emit"
      pattern: "--color-(bg|primary|radiant|dire)"
---

<objective>
Wave 0 foundation for the Tactical Slate restyle. Three blocking prerequisites that every later wave depends on:
1. Replace the 2-token `@theme` in `client/src/index.css` with the full Tactical Slate token layer (~18 color tokens + fonts + radii + shadow + tracking), copied verbatim from the LOCKED `tactical-slate.css`. Put gradient/glow/soft-tint values in plain `:root` (not `@theme`) so they don't generate junk utilities.
2. Wire Inter + JetBrains Mono via a Google Fonts `<link>` in `client/index.html` with `display=swap` (no FOUT/FOIT, no build break).
3. Pre-emptively fix the 6 assertions in `HistoryGraphs.test.tsx` that hard-assert the retired literals `#4ade80` / `#ef4444` / `rgb(74, 222, 128)` / `rgb(239, 68, 68)`, and swap HistoryGraphs.tsx color consts (lines 9-10) to the softened team colors so the suite stays green NOW. (HistoryGraphs.tsx's full panel/axis retokening is owned by Plan 05.)

Purpose: Establishes the single source of truth for all token utilities and runtime `var(--...)` refs (UI-01), and removes the one known test landmine before any component touches the new palette (success criterion 5).
Output: Token layer present, fonts loaded, suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md
@.planning/sketches/themes/tactical-slate.css

<interfaces>
<!-- The LOCKED token source. COPY these values into index.css @theme / :root. Do NOT @import the sketch file. -->
From .planning/sketches/themes/tactical-slate.css (canonical, LOCKED):
  Colors (to @theme, generate bg-*/text-*/border-*/fill-* utilities):
    --color-bg: #14151a;            --color-bg-elev: #181a20;
    --color-surface: #1d1f27;       --color-surface-2: #23262f;
    --color-border: #2b2e38;        --color-border-glow: #3a3e4a;
    --color-text: #e6e7ea;          --color-text-muted: #9aa0ad;  --color-text-dim: #646b78;
    --color-primary: #d4a24a;       --color-accent: #5b9bd5;
    --color-radiant: #6bcf8a;       --color-dire: #e06a72;
    --color-gold: #d4a24a;          --color-danger: #e06a72;
  Fonts (to @theme, generate font-sans/font-mono):
    --font-sans: 'Inter', system-ui, sans-serif;
    --font-mono: 'JetBrains Mono', ui-monospace, monospace;
  Radii (to @theme, generate rounded-sm/md/lg/full):
    --radius-sm: 6px; --radius-md: 10px; --radius-lg: 16px; --radius-full: 9999px;
  Shadow + tracking (to @theme): --shadow-card: 0 6px 20px rgba(0,0,0,0.35); --tracking-label: 0.14em;
  var()-ONLY values (to plain :root, NOT @theme — Pitfall 4):
    --color-primary-soft: rgba(212,162,74,0.12);  --color-accent-soft: rgba(91,155,213,0.12);
    --color-radiant-soft: rgba(107,207,138,0.12); --color-dire-soft: rgba(224,106,114,0.12);
    --card-bg-grad: linear-gradient(180deg, #20232c 0%, #1a1c23 100%);
    --glow-primary: 0 0 0 1px rgba(212,162,74,0.30); --glow-accent: 0 0 0 1px rgba(91,155,213,0.30);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace index.css @theme with the full Tactical Slate token layer</name>
  <read_first>
    - client/src/index.css (current 2-token @theme — full replace)
    - .planning/sketches/themes/tactical-slate.css (LOCKED token source — copy values verbatim)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md sections "Design Tokens" + "Typography" (prose scale)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md sections "Pattern 1" + "Pitfall 4" (@theme vs :root)
  </read_first>
  <action>
    Overwrite `client/src/index.css`. Keep `@import "tailwindcss";` as line 1.
    Replace the existing `@theme { --color-radiant: #4ade80; --color-dire: #ef4444; }` block with a full `@theme` block containing EXACTLY these tokens (copy values from the interfaces block above, sourced from tactical-slate.css):
      Colors: --color-bg #14151a, --color-bg-elev #181a20, --color-surface #1d1f27, --color-surface-2 #23262f, --color-border #2b2e38, --color-border-glow #3a3e4a, --color-text #e6e7ea, --color-text-muted #9aa0ad, --color-text-dim #646b78, --color-primary #d4a24a, --color-accent #5b9bd5, --color-radiant #6bcf8a, --color-dire #e06a72, --color-gold #d4a24a, --color-danger #e06a72.
      Fonts: --font-sans 'Inter', system-ui, sans-serif; --font-mono 'JetBrains Mono', ui-monospace, monospace.
      Radii: --radius-sm 6px, --radius-md 10px, --radius-lg 16px, --radius-full 9999px.
      Effects: --shadow-card 0 6px 20px rgba(0,0,0,0.35); --tracking-label 0.14em.
      Prose text sizes (per UI-SPEC Typography — tokenized so the grep stays clean): --text-label 11px, --text-body 14px, --text-heading 20px. (Display uses clamp inline at the h1 — no token needed.)
      Named breakpoint for the 1180px stack threshold (Open Question 2 — chosen named approach): --breakpoint-stack 1180px. This generates stack: / max-stack: variants reused across Home + Match.
    After the @theme block, add a plain :root { ... } block (NOT @theme — these must NOT generate utilities, Pitfall 4) with the var()-only values: --color-primary-soft rgba(212,162,74,0.12), --color-accent-soft rgba(91,155,213,0.12), --color-radiant-soft rgba(107,207,138,0.12), --color-dire-soft rgba(224,106,114,0.12), --card-bg-grad linear-gradient(180deg, #20232c 0%, #1a1c23 100%), --glow-primary 0 0 0 1px rgba(212,162,74,0.30), --glow-accent 0 0 0 1px rgba(91,155,213,0.30).
    Do NOT add dark: variants or a tailwind.config.js — Tailwind 4 is CSS-first and the app is always-dark.
    Restart the dev server once after this edit if utilities don't appear (build-time generation).
  </action>
  <verify>
    <automated>cd client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - client/src/index.css contains @theme and the literal strings "--color-bg: #14151a", "--color-primary: #d4a24a", "--color-radiant: #6bcf8a", "--color-dire: #e06a72", "--breakpoint-stack: 1180px", "--card-bg-grad".
    - client/src/index.css no longer contains #4ade80 or #ef4444.
    - The var()-only tokens (--color-radiant-soft, --card-bg-grad, --glow-primary) live in a plain :root block, not inside @theme.
    - cd client && npm run build exits 0 (tsc + vite build green).
  </acceptance_criteria>
  <done>Full Tactical Slate token layer present; old radiant/dire literals gone; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Inter + JetBrains Mono via Google Fonts link</name>
  <read_first>
    - client/index.html (current head — no font links)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md section "Adding the Google Fonts link"
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md section "Typography" (Inter 500/600, JetBrains Mono 700/800)
  </read_first>
  <action>
    In client/index.html head, before title, add:
      <link rel="preconnect" href="https://fonts.googleapis.com" />
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@500;600&family=JetBrains+Mono:wght@700;800&display=swap" rel="stylesheet" />
    Weights MUST match UI-SPEC: Inter 500;600, JetBrains Mono 700;800. display=swap keeps the system-ui fallback visible until Inter loads (no FOIT). The --font-sans/--font-mono tokens from Task 1 already reference these families with system-ui/ui-monospace fallbacks. Do NOT add @fontsource npm deps.
  </action>
  <verify>
    <automated>cd client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - client/index.html contains fonts.googleapis.com, fonts.gstatic.com, "family=Inter:wght@500;600", "JetBrains+Mono:wght@700;800", and display=swap.
    - crossorigin is present on the gstatic preconnect link.
    - cd client && npm run build exits 0.
  </acceptance_criteria>
  <done>Both font families load via link with swap; build green; no npm font deps added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retarget HistoryGraphs.test.tsx assertions + swap HistoryGraphs.tsx color consts</name>
  <read_first>
    - client/src/components/HistoryGraphs.test.tsx (the 6 assertions at lines 72, 75, 94, 96, 116, 135)
    - client/src/components/HistoryGraphs.tsx (consts RADIANT_GREEN='#4ade80', DIRE_RED='#ef4444' at lines 9-10)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md section "Pitfall 1" + Open Question 1 (assertion strategy)
  </read_first>
  <behavior>
    - Decision (consistent across the whole SVG-bearing component set): HistoryGraphs.tsx keeps its team color consts as LITERAL softened hex (#6bcf8a / #e06a72), NOT var(). Rationale: jsdom does not resolve CSS vars, so asserting literal hex on fill/style.color stays deterministic. Plan 05 honors this same const-literal choice when finishing HistoryGraphs.
    - Test (UAT-CHART-01): Radiant peak text fill === #6bcf8a; a circle[r="3.5"] has fill === #6bcf8a.
    - Test (UAT-CHART-02): Dire peak text fill === #e06a72; a circle[r="3.5"] has fill === #e06a72.
    - Test (UAT-CHART-03): Radiant headline style.color === #6bcf8a OR rgb(107, 207, 138); Dire headline === #e06a72 OR rgb(224, 106, 114).
  </behavior>
  <action>
    Edit client/src/components/HistoryGraphs.test.tsx:
      - Line 72: toBe('#4ade80') to toBe('#6bcf8a')
      - Line 75: c.getAttribute('fill') === '#4ade80' to === '#6bcf8a'
      - Line 94: toBe('#ef4444') to toBe('#e06a72')
      - Line 96: c.getAttribute('fill') === '#ef4444' to === '#e06a72'
      - Line 116: color === '#4ade80' || color === 'rgb(74, 222, 128)' to color === '#6bcf8a' || color === 'rgb(107, 207, 138)'
      - Line 135: color === '#ef4444' || color === 'rgb(239, 68, 68)' to color === '#e06a72' || color === 'rgb(224, 106, 114)'
    Do NOT change any other assertion (peak-text regex, anchor, gridline, no-hover, Russian skeleton text). Leave Russian copy strings untouched (restyle != recopy — RESEARCH A1).
    Edit client/src/components/HistoryGraphs.tsx ONLY lines 9-10: RADIANT_GREEN = '#6bcf8a'; DIRE_RED = '#e06a72'. This 2-line const swap makes the test green NOW. Do NOT touch PANEL_BG/PANEL_BORDER/SECONDARY_FG/TERTIARY_FG/ZERO_AXIS/GRID_LINE or anything else in HistoryGraphs.tsx — those are Plan 05's scope.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "#4ade80" client/src/components/HistoryGraphs.test.tsx returns 0; grep -c "#ef4444" returns 0.
    - HistoryGraphs.test.tsx contains #6bcf8a, "rgb(107, 207, 138)", #e06a72, "rgb(224, 106, 114)".
    - HistoryGraphs.tsx lines 9-10 contain RADIANT_GREEN = '#6bcf8a' and DIRE_RED = '#e06a72'.
    - cd client && npx vitest run exits 0 (105 tests green; HistoryGraphs 13 green).
  </acceptance_criteria>
  <done>HistoryGraphs test asserts softened colors; component team consts swapped to match; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser to Google Fonts CDN | new third-party origin for font files (read-only static assets) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-01 | I (Information disclosure) | Google Fonts link | accept | Static public CSS/woff2 only; no user data sent beyond standard request headers. CSP allowlist deferred to Phase 11 (RESEARCH Security Domain). |
| T-10.4-02 | T (Tampering) | index.css token layer | accept | Build-time CSS; no user input, no runtime mutation. Pure cosmetic. |

No new attack surface: CSS/markup-only restyle; no user input, no new network calls beyond a static font CDN; no auth/data flow change.
</threat_model>

<verification>
- cd client && npx vitest run -> 105 tests green.
- cd client && npm run build -> tsc + vite build green.
- client/src/index.css token grep passes (Task 1 check).
- client/index.html font-link grep passes (Task 2 check).
</verification>

<success_criteria>
Full Tactical Slate @theme token layer + var()-only :root block present in index.css; Inter + JetBrains Mono load via link; HistoryGraphs.test asserts softened colors, HistoryGraphs.tsx team consts swapped, and the full 105-test suite is green; build green. Unblocks all Wave 1+ migrations.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-01-SUMMARY.md` documenting: final token names emitted as utilities, the named `stack:` breakpoint, the const-literal (not var()) decision for SVG-bearing components, and confirmation the suite is green.
</output>
