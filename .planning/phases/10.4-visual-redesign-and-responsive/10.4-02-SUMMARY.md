---
phase: 10.4-visual-redesign-and-responsive
plan: 02
subsystem: frontend-home
tags: [tailwind4, css-tokens, responsive, homepage, matchrow, vitest]
requires:
  - "Tactical Slate @theme token layer in client/src/index.css (Plan 01)"
  - "Inter + JetBrains Mono fonts wired in index.html (Plan 01)"
provides:
  - "Tokenized + responsive Home shell, header (live indicator), empty/error/loading states"
  - "Tokenized StatusTag, SkeletonRow, ErrorBanner (standardized copy)"
  - "LeagueAccordion + MatchRow with CSS gold hover rail (imperative->CSS) and phone 2-row collapse, 44px tap target"
affects: [home-page, match-list]
tech-stack:
  added: []
  patterns:
    - "Imperative JS hover (.style mutation) -> CSS group-hover/hover utility classes"
    - "Responsive collapse via Tailwind breakpoint utilities (flex-col sm:flex-row, truncate + min-w-0)"
key-files:
  created:
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-02-SUMMARY.md
  modified:
    - client/src/pages/HomePage.tsx
    - client/src/components/LeagueAccordion.tsx
    - client/src/components/MatchRow.tsx
    - client/src/components/StatusTag.tsx
    - client/src/components/SkeletonRow.tsx
    - client/src/components/ErrorBanner.tsx
key-decisions:
  - "MatchRow hover rail migrated from imperative onMouseEnter/.style.background to CSS group-hover:bg-primary on a w-[3px] left rail + hover:bg-surface-2 on the row"
  - "Phone 2-row collapse via sm:flex-row with truncate + min-w-0 to protect against overflow at 320px"
  - "ErrorBanner copy standardized to the UI-SPEC error string with var(--color-danger)"
requirements: [UI-01, UI-02]
duration: 6min
completed: 2026-06-07
---

# Phase 10.4 Plan 02: Home Retoken + Responsive Summary

**Migrated the entire Home screen (sketch 003-B) to Tactical Slate tokens with zero color-hex literals and made it responsive across the four target widths: HomePage shell/header/empty/error/loading states, LeagueAccordion, MatchRow (imperative-hover -> CSS hover rail + phone 2-row collapse), StatusTag, SkeletonRow, and ErrorBanner — all behavior preserved, 105-test suite green, build clean.**

## Performance
- **Duration:** ~6 min
- **Tasks:** 2 / 2 completed
- **Files modified:** 6

## What Was Built

### Task 1 — Home shell, header, states + StatusTag/SkeletonRow/ErrorBanner
- HomePage: removed `bg-gray-950`/`text-green-400` legacy classes; root bg/text on Tactical Slate tokens; header live indicator shows "live now" + `animate-pulse`; empty/error/loading states retoned to tokens.
- StatusTag, SkeletonRow retoned to surface/border/text tokens.
- ErrorBanner: cosmetic retoken to `var(--color-danger)` + standardized copy.

### Task 2 — LeagueAccordion + MatchRow
- MatchRow: imperative `onMouseEnter/onMouseLeave` `.style.background` handlers removed; replaced with CSS `group-hover:bg-primary` on a `w-[3px]` hover rail and `hover:bg-surface-2` on the row; `sm:flex-row` phone 2-row collapse with `truncate` + `min-w-0`; 44px tap target preserved.
- LeagueAccordion retoned to tokens; expand/collapse behavior unchanged.

## Verification

- `grep -E "#[0-9a-fA-F]{3,6}"` across all 6 Home files → **0 matches**.
- MatchRow: `group-hover:bg-primary`, `w-[3px]`, `hover:bg-surface-2`, `sm:flex-row`, `truncate` + `min-w-0` present; no imperative hover handlers.
- HomePage: "live now" + `animate-pulse` present; no `bg-gray-950`/`text-green-400`. ErrorBanner: standardized copy + `var(--color-danger)`.
- `cd client && npx vitest run` → **105 passed (15 files)**, exit 0. `npm run build` (tsc + vite) → exit 0.

Responsive reflow (phone 2-row collapse) is layout behavior jsdom cannot assert — validated at the Wave 4 UAT checkpoint (Plan 08).

## Deviations from Plan

None — executed as written. (SUMMARY.md was authored by the orchestrator after the executor's worktree-mode write of this file was blocked by the session permission layer; both code tasks were committed atomically by the executor before that block — see Self-Check commits.)

## Known Stubs

None — presentational restyle of existing wired data only.

## Self-Check: PASSED

- FOUND: all 6 Home files modified, committed in 097f6ba (Task 1) and b42aee0 (Task 2)
- FOUND: .planning/phases/10.4-visual-redesign-and-responsive/10.4-02-SUMMARY.md
- Commits 097f6ba and b42aee0 present in git log (merged via 7b66cd2).
