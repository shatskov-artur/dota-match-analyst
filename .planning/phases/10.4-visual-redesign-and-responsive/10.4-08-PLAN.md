---
phase: 10.4-visual-redesign-and-responsive
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UAT.md
autonomous: false
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "No color-hex literals remain across client/src components + pages (except documented allowlist)"
    - "Full suite + build green after all migrations"
    - "Home (003-B) and Match (004-B) match the sketches visually at desktop"
    - "All 4 breakpoints (375/768/1280/1536) reflow with no horizontal scroll, overlap, or clipping; dense data legible on phone"
  artifacts:
    - path: ".planning/phases/10.4-visual-redesign-and-responsive/10.4-UAT.md"
      provides: "UAT sign-off record for the visual + responsive gate"
      contains: "UAT"
  key_links:
    - from: "all migrated components"
      to: "sketches 003-B / 004-B + 4-width reflow"
      via: "manual visual verification"
      pattern: "approved"
---

<objective>
Final phase gate: run the full automated sweep (no-hex grep across all client components/pages, full suite, build), then a human manual-UAT checkpoint comparing Home vs sketch 003-B and Match vs sketch 004-B at desktop, and verifying responsive reflow at all four target widths (375/768/1280/1536) with no horizontal scroll, overlap, or clipping, and dense data legible on phone. Record the result in 10.4-UAT.md.

Purpose: Closes UI-01 (token migration complete, verified), UI-02/UI-03 (responsive correctness — the irreducible manual gate, since jsdom has no layout engine).
Output: 10.4-UAT.md sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-VALIDATION.md
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated sweep — no-hex grep, suite, build</name>
  <read_first>
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-VALIDATION.md section "Success Criterion to Validation Map" (grep commands)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md section "Verification grep" (allowlist of fixed-glyph + rgba-black scrim residuals)
  </read_first>
  <action>
    Run the phase-wide automated checks and capture results for the UAT record:
      1. Color-hex sweep across all client components + pages. Run from repo root:
         grep -rnE "#[0-9a-fA-F]{3,6}" client/src/components client/src/pages --include=*.tsx
         Expected residuals (ALLOWLIST — acceptable, NOT failures):
           - client/src/components/HistoryGraphs.tsx: the two literal-softened team consts #6bcf8a and #e06a72 (test-required; jsdom can't resolve var()).
           - client/src/components/HistoryGraphs.test.tsx and RoshanBlock.test.tsx: test assertion literals (test files, not shipped UI).
         Any OTHER #hex in a .tsx component/page is a FAILURE — fix before sign-off (return to the owning plan).
      2. rgba color sweep (informational): grep -rnE "rgba\(" client/src/components client/src/pages --include=*.tsx
         Expected residuals (ALLOWLIST): decorative black scrims rgba(0,0,0,*) in DraftPortrait/DraftTimeline (badge/drop-shadow). Any rgba PALETTE color (non-black) is a FAILURE.
      3. Full suite: cd client && npx vitest run  (expect 105 passed).
      4. Build: cd client && npm run build  (expect tsc + vite build green, exit 0).
    Record the exact grep output (or "clean except allowlist") + suite count + build status into 10.4-UAT.md (Task 2 creates the file). If any non-allowlisted hex/rgba or any test/build failure is found, STOP and report which plan owns the offending file — do not proceed to the human checkpoint until clean.
  </action>
  <verify>
    <automated>cd client && npx vitest run && cd client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - "grep -rnE \"#[0-9a-fA-F]{3,6}\" client/src/components client/src/pages --include=*.tsx" returns ONLY allowlisted residuals (HistoryGraphs.tsx #6bcf8a/#e06a72 + *.test.tsx files). Zero non-allowlisted matches.
    - No rgba() PALETTE colors (non-black) remain in components/pages (only rgba(0,0,0,*) scrims allowed).
    - cd client && npx vitest run exits 0 (105 passed).
    - cd client && npm run build exits 0 (tsc + vite build).
  </acceptance_criteria>
  <done>Automated gate green: no-hex sweep clean (allowlist only), 105 tests pass, build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT at 4 breakpoints + sketch fidelity (human checkpoint)</name>
  <what-built>
    The entire app has been restyled to Tactical Slate via a shared @theme token layer (Plans 01-07): Home (shell, header live indicator, league accordion, match rows, status tags), and Match (score header, win-prob, heroes/items/cooldowns Row 1, history/roshan+buildings/map Row 2, draft section, intel tooltip). Fonts (Inter + JetBrains Mono) load via Google Fonts. Responsive reflow flips dense grids to single column at 1180px; the map is fluid. All 105 client tests + build are green and the no-hex sweep is clean (Task 1).
  </what-built>
  <how-to-verify>
    Start the dev server: cd client && npm run dev (note the localhost URL, typically http://localhost:5173).
    Open a browser with DevTools responsive mode.

    A) DESKTOP SKETCH FIDELITY (~1536px):
      1. Home: compare against .planning/sketches/003-home-redesign/ (theme = tactical-slate). Confirm warm-charcoal bg, gold wordmark, soft-shadow rounded league cards, gold hover rail on match rows, Live = dire-red tag / Draft = gold tag.
      2. Match (open a LIVE tournament match if one is available; otherwise a recent match route): compare against .planning/sketches/004-match-page-tactical-slate/. Confirm 56px mono kill scores, steel roshan pill, gold gold-diff, team-ringed portraits, gold-tinted item slots, steel cooldown timers, slate history charts with static peak labels (NO hover tooltips), fluid map with radiant/dire blips.

    B) REFLOW AT 4 WIDTHS — for BOTH Home and Match, at each width confirm: NO horizontal scrollbar, NO overlapping/clipped content, block order unchanged:
      1. Phone 375px: Home match rows collapse to 2 internal rows (teams row + meta row), team names truncate not clip. Match: score header stacks 3 rows (kill score 40px), Row 1 stacks heroes/items/cooldowns, Row 2 stacks history/roshan+buildings/map, map caps at card width.
      2. Tablet 768px: layouts readable, map does not force scroll.
      3. Laptop 1280px: full multi-column Match layout intact (above the 1180px stack threshold = 3-col rows).
      4. Desktop 1536px: max-width respected (Home 1180, Match 1320 centered), no excessive whitespace.

    C) DENSE-DATA LEGIBILITY ON PHONE (375px) — confirm: hero portraits readable, all 6 item slots present per player (not wrapped/dropped), cooldown icons + steel timers legible, all numerics >= 12px with tabular-nums alignment.

    D) NO BEHAVIOR REGRESSION: confirm the match polls/updates, building dots render (or graceful placeholder when building_state absent), and finished-match polling still stops (game_state 6) — the restyle must not have changed behavior.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failing item (which screen, which width, what's wrong) so the owning plan can be revisited. On approval, write the sign-off to .planning/phases/10.4-visual-redesign-and-responsive/10.4-UAT.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record UAT sign-off</name>
  <read_first>
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-VALIDATION.md section "Manual-Only Verifications"
  </read_first>
  <action>
    After human approval (Task 2 resume-signal "approved"), create .planning/phases/10.4-visual-redesign-and-responsive/10.4-UAT.md recording:
      - Automated gate results from Task 1 (no-hex sweep status, 105/105 test count, build status).
      - Manual UAT outcome per the VALIDATION.md Manual-Only table: Home 003-B fidelity, Match 004-B fidelity, reflow at 375/768/1280/1536, dense-data legibility on phone — each marked PASS or with noted defects.
      - Any defects found + the plan they were routed back to (if any).
      - Final sign-off line with date and "approved".
    If the human reported defects instead of "approved", record them in 10.4-UAT.md as OPEN and do NOT mark the phase complete — the orchestrator routes a gap-closure plan.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/10.4-visual-redesign-and-responsive/10.4-UAT.md';if(!fs.existsSync(p)){console.error('UAT missing');process.exit(1)}console.log('UAT recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UAT.md exists.
    - It records the automated gate (test count + build) and the 4-width manual reflow outcomes.
    - It contains a dated sign-off (approved) OR a list of OPEN defects with routing.
  </acceptance_criteria>
  <done>10.4-UAT.md written with automated + manual results and a clear approved/open verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Verification + documentation only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-10 | (n/a) | UAT verification | accept | No code change; verification + doc only. No attack surface. |

No new attack surface: verification + documentation only; no user input, no new network calls; no auth/data flow change.
</threat_model>

<verification>
- Automated gate: no-hex sweep clean (allowlist only), 105 tests green, build green.
- Human checkpoint: Home 003-B + Match 004-B fidelity at desktop; reflow at 375/768/1280/1536 with no h-scroll/overlap/clip; dense-data legible on phone; no behavior regression.
- 10.4-UAT.md records the verdict.
</verification>

<success_criteria>
The no-hex sweep is clean across all client components/pages (allowlist only), full suite + build are green, and a human has verified sketch fidelity and responsive reflow at all four widths with no horizontal scroll/overlap/clipping and legible dense data — recorded in 10.4-UAT.md. This closes UI-01/UI-02/UI-03 and the phase success criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-08-SUMMARY.md` summarizing the gate outcome and linking 10.4-UAT.md.
</output>
