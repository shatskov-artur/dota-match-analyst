---
phase: 10.4-visual-redesign-and-responsive
plan: 06
subsystem: frontend-draft-intel
tags: [tailwind4, css-tokens, draft, intel-tooltip, vitest]
requires:
  - "Tactical Slate @theme token layer in client/src/index.css (Plan 01)"
provides:
  - "Tokenized draft section: DraftSection, DraftTurnIndicator, DraftColumn, DraftPortrait, DraftTimeline"
  - "Tokenized IntelTooltip floating card (slate card, surface-2 placeholders, dire warning flag)"
  - "Legacy ember-red active accents retoned to gold primary; team identity stays team-colored"
affects: [match-page, draft-section]
tech-stack:
  added: []
  patterns:
    - "Ember-red active-slot accent (#b03030 / rgba(176,48,48,*)) -> gold var(--color-primary) / var(--color-primary-soft)"
    - "Allowlisted exceptions kept: winrateColor() util calls and decorative black scrims (rgba(0,0,0,*)) are not palette colors"
key-files:
  created:
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-06-SUMMARY.md
  modified:
    - client/src/components/DraftTurnIndicator.tsx
    - client/src/components/DraftColumn.tsx
    - client/src/components/DraftPortrait.tsx
    - client/src/components/DraftTimeline.tsx
    - client/src/components/IntelTooltip.tsx
key-decisions:
  - "Active-slot emphasis retoned from legacy ember red to GOLD primary (var(--color-primary) borders, var(--color-primary-soft) glow) per slate palette; team colors (radiant/dire) stay team-colored, including the ban-X which stays dire red"
  - "Allowlisted and intentionally kept: winrateColor(heroStats.win_rate) JS calls (winrate-tier util, not chrome) and decorative black scrims rgba(0,0,0,0.72)/(0.7)/(0.8) (badge bg + drop/text shadows)"
  - "DraftSection required no change (composition only, already hex-free)"
  - "IntelTooltip hidden-profile em-dash copy (PLAYER-02) and useLayoutEffect flip threshold (>=180) preserved verbatim"
requirements: [UI-01]
duration: split (Task 1 worktree executor; Tasks 2-3 inline orchestrator)
completed: 2026-06-07
---

# Phase 10.4 Plan 06: Draft Section + IntelTooltip Retoken Summary

**Migrated all six draft/intel components (sketch 004-B palette) to Tactical Slate tokens with zero color-hex literals (excluding allowlisted black scrims + the winrateColor util): DraftTurnIndicator, DraftColumn, DraftPortrait, DraftTimeline, and IntelTooltip. Legacy ember-red active-slot accents are now gold primary; team identity (radiant/dire, ban-X) stays team-colored. CM 7.40 ordering, badge strips, ban-X overlay, ordinal badges, tooltip hover + flip positioning, and hidden-profile handling are all preserved; 105-test suite green.**

## Performance
- **Tasks:** 3 / 3 completed
- **Files modified:** 5 (DraftSection needed no change)

## Execution note
Task 1 (DraftTurnIndicator/DraftColumn, plus DraftSection verification) was completed and committed by the parallel worktree executor (`07ebebe`, merged via `6c7684f`). Tasks 2 (DraftPortrait/DraftTimeline) and 3 (IntelTooltip) were completed inline by the orchestrator (`8ec1568`, `88326f8`) after the session permission environment blocked the executor's `.tsx` edits mid-task (per user decision "Finish inline, no subagents").

## What Was Built

### Task 1 — DraftTurnIndicator + DraftColumn (+ DraftSection)
- DraftTurnIndicator: labels → `var(--color-text-dim)`; active team label → `var(--color-radiant)`/`var(--color-dire)`; gameState gate + getPhaseName + tentative opacity preserved.
- DraftColumn: team labels → team tokens; ember `borderLeft`/`boxShadow` → `var(--color-primary)`/`var(--color-primary-soft)`; active bg → `var(--color-surface-2)`; picks/bans divider → `var(--color-border)`; isActive/tentative + 5-pick/7-ban structure preserved.
- DraftSection: confirmed hex-free, no change.

### Task 2 — DraftPortrait + DraftTimeline
- Placeholders → `var(--color-surface)`; active border → `var(--color-primary)`; inactive → `var(--color-border)`; ban-X → `var(--color-dire)` (black drop-shadow scrim kept); badge suffix/ordinal → `var(--color-text-muted)`; DraftTimeline R/D row labels → team tokens; step number → `var(--color-text-dim)`; phase divider → `var(--color-border)`.
- Preserved: winrateColor badges, IntelTooltip state triggers, Pitfall-4 overflow structure, PHASE_DIVIDER_BEFORE set, two-row chess layout, portraitRefs/flip logic.

### Task 3 — IntelTooltip
- Card bg → `var(--card-bg-grad)`; border → `var(--color-border)`; shadow → `var(--shadow-card)`; radius → `var(--radius-md)`; name/counter name → `var(--color-text)`; stat lines → `var(--color-text-muted)`; section labels → `var(--color-text-dim)`; separators → `var(--color-border)`; loading/counter placeholders → `var(--color-surface-2)`; warning flag → `var(--color-dire)`.
- Preserved: useLayoutEffect flip (rect.top >= 180), positionStyle, pointerEvents none, hidden-profile em-dash "—% on {hero}" copy (PLAYER-02), counters map.

## Verification

- `grep -E "#[0-9a-fA-F]{3,6}"` → **0** for all 6 files (DraftSection/TurnIndicator/Column/Portrait/Timeline/IntelTooltip).
- DraftColumn `var(--color-primary)` ×2; DraftPortrait `var(--color-primary)` ×1; DraftTimeline `var(--color-radiant)`+`var(--color-dire)`; IntelTooltip `var(--card-bg-grad)` + `var(--color-surface-2)` ×2.
- winrateColor preserved (DraftPortrait ×2, DraftTimeline ×2). IntelTooltip `useLayoutEffect` + `180` present.
- `cd client && npx vitest run` → **105 passed (15 files)**, exit 0.

## Deviations from Plan

None to the planned changes. Process deviation: Tasks 2-3 completed inline due to the permission environment. The DraftTimeline ban-X edit that was originally blocked (`#ef4444` → `var(--color-dire)`) is now applied.

## Known Stubs

None — presentational restyle of existing wired data only.

## Self-Check: PASSED

- FOUND: all 5 modified draft/intel files committed in 07ebebe (T1), 8ec1568 (T2), 88326f8 (T3)
- FOUND: .planning/phases/10.4-visual-redesign-and-responsive/10.4-06-SUMMARY.md
- Commits 07ebebe, 8ec1568, 88326f8 present in git log.
