---
phase: 10.4-visual-redesign-and-responsive
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - client/src/pages/MatchPage.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "MatchPage shell, back-link, h1, and ambient glow render in Tactical Slate tokens with no inline color hex"
    - "Back-link hover uses CSS (color to primary), no imperative .style"
    - "Match in-game grid reflows: Row 1 (heroes/items/cooldowns) stacks below 1180px; Row 2 (history/roshan+buildings/map) stacks below 1180px; map is fluid; no horizontal scroll at 375/768"
    - "Phase 10.3 three-row block order and all behavior (polling, building_state guard, gates) preserved"
  artifacts:
    - path: "client/src/pages/MatchPage.tsx"
      provides: "Tokenized + responsive Match assembly"
      contains: "max-stack:flex-col"
  key_links:
    - from: "MatchPage Row 1 + Row 2 flex containers"
      to: "stack threshold"
      via: "max-stack: / stack: variants (1180px)"
      pattern: "stack:"
---

<objective>
Final Match-page assembly: retoken MatchPage.tsx shell (root bg/text, ambient glow, back-link incl. imperative-hover removal, h1 title) to Tactical Slate, and apply the UI-SPEC Match-page responsive reflow — Row 1 (heroes | items | cooldowns) and Row 2 (history | roshan+buildings | map) both flip from multi-column to stacked single-column at the canonical 1180px threshold, with the map fluid and min-w-0 discipline preventing dense-panel overflow. Preserve the Phase 10.3 three-row block order and ALL behavior (polling-stop, building_state guard, in-game gate, draft mount gate).

Purpose: UI-01 (MatchPage shell tokens), UI-02/UI-03 (Match reflow at all 4 widths). This is the assembly that depends on every component migration (Plans 02-06).
Output: Tokenized, fully responsive Match page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/sketches/themes/tactical-slate.css
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-01-SUMMARY.md
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Token utilities + variants (Plan 01):
  bg-bg text-text text-text-dim  font-sans  rounded-lg
  var()-only: var(--color-bg) var(--color-text) var(--color-text-dim) var(--color-primary)
Stack breakpoint variants (Plan 01 named --breakpoint-stack: 1180px):
  stack:  = min-width 1180px   ;   max-stack: = max-width 1179px
Literal to token map (MatchPage current inline styles):
  root background '#0a0a0a' to var(--color-bg) (or className bg-bg); color '#d8d8d8' to var(--color-text)
  ambient glow rgba(176,48,48,0.03) to a NEUTRAL retint: drop the red, use var(--color-primary-soft) at very low alpha OR remove the glow (UI-SPEC: "swap ambient red glow to drop or retint to neutral"). Prefer subtle gold: radial-gradient(... var(--color-primary-soft) 0%, transparent 100%).
  back-link color '#666666' to var(--color-text-dim); hover '#e05050' to var(--color-primary) via CSS hover (remove imperative)
  h1 vs separator '#3a3a3a' to var(--color-text-dim); h1 color '#ffffff' to text-text (or keep pure white for Display per UI-SPEC Display = pure white #fff — acceptable, but prefer text-text token; UI-SPEC Display says pure white #fff — use #fff is allowed ONLY for Display h1, document in SUMMARY). To keep the grep clean, use text-text.
Current Row layouts to make responsive:
  Row 1: "flex gap-8 items-stretch" with 3x "flex-1 min-w-0" children -> phone-first stack
  Row 2: "flex gap-8 items-start" with [history flex-1 min-w-0] [roshan+buildings shrink-0 w-[320px]] [map shrink-0 size=420] -> stack at 1180px, map fluid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retoken MatchPage shell, back-link (CSS hover), ambient glow, h1</name>
  <read_first>
    - client/src/pages/MatchPage.tsx (root style lines 34-37; ambient glow 41-44; back-link imperative hover 50-53; h1 60-70)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md sections "Component Inventory" (MatchPage), "Typography" (Display h1 clamp), "Interaction Contract" (back link hover to primary 160ms), "Copywriting" (Back to matches)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md section "Pattern 3" (imperative to CSS hover)
  </read_first>
  <action>
    MatchPage.tsx shell:
      - Root div: replace style {{ background: '#0a0a0a', color: '#d8d8d8' }} with className additions bg-bg text-text font-sans (drop inline style). Add responsive page gutters: change "p-8" to "px-4 md:px-6 lg:px-8 py-8" and center with max-w-[1320px] mx-auto (UI-SPEC Match page max-width 1320px).
      - Ambient glow div: retint from red rgba(176,48,48,0.03) to neutral gold: background 'radial-gradient(ellipse 60% 40% at 50% 0%, var(--color-primary-soft) 0%, transparent 100%)'. Keep pointer-events-none + positioning.
      - Back-link: REMOVE the imperative onMouseEnter/onMouseLeave (lines 52-53) and the inline color style. Replace with className: keep "inline-flex items-center gap-2 mb-10 text-[11px] uppercase tracking-[0.25em]" and ADD "text-text-dim transition-colors duration-150 hover:text-primary". Keep the copy. (UI-SPEC: "Back to matches", hover to primary 160ms; duration-150 is acceptable.)
      - h1: replace style fontSize 'clamp(1.8rem, 5vw, 3.5rem)' with the UI-SPEC Display clamp 'clamp(1.8rem, 4vw, 2.8rem)'; color '#ffffff' to text-text via className (drop inline color); keep letterSpacing -0.02em (Display tracking) as inline (non-color). The " vs " separator span color '#3a3a3a' to var(--color-text-dim). Allow wrap to 2 lines on phone (default behavior; ensure no nowrap).
    Preserve all hooks, the draft mount gate (draft.scoreboard), the in-game gate (game_state === 5 && radiantPlayers.length > 0), the pre-game HeroPlayerGrid path, playerIntelMap construction, and the buildings.unavailable guard. Do NOT touch polling/useMatchDetail logic.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -E for hex in MatchPage.tsx returns 0 matches.
    - grep -cE "onMouseEnter|onMouseLeave|.style." in MatchPage.tsx returns 0 (imperative back-link hover removed).
    - MatchPage contains "hover:text-primary" (CSS back-link hover) and "bg-bg".
    - MatchPage h1 contains "clamp(1.8rem, 4vw, 2.8rem)".
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>MatchPage shell/back-link/glow/h1 tokenized with CSS hover and neutral glow; gates + hooks preserved; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Match in-game responsive reflow (Row 1 + Row 2 stack at 1180px, map fluid)</name>
  <read_first>
    - client/src/pages/MatchPage.tsx (Row 1 container line 124; Row 2 container line 152; map DotaMapView size=420 line 167)
    - client/src/components/DotaMapView.tsx (now fluid after Plan 05 — size prop is the max-width cap)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md section "Match page reflow" (Row 1 + Row 2 tables — apply VERBATIM)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md sections "Pattern 4" (mobile-first reflow), "Pitfall 2" (min-w-0 mandatory), "Pitfall 3" (1180px threshold)
  </read_first>
  <action>
    Apply the UI-SPEC Match-page reflow to the in-game block (the section gated by game_state === 5 && radiantPlayers.length > 0). Use the named stack variants from Plan 01 (stack: = min-width 1180px). Mobile-first: base = stacked, stack: = multi-column.
    Row 1 (heroes | items | cooldowns), currently "flex gap-8 items-stretch":
      - Change to "flex flex-col gap-6 stack:flex-row stack:gap-8 stack:items-stretch".
      - Each of the 3 children keeps "min-w-0" (mandatory anti-overflow) and gains "stack:flex-1" (full width when stacked, equal flex when row). Keep the inner flex flex-col on items/cooldowns wrappers.
      - Per UI-SPEC tablet note: at 768px prefer 2-col (heroes+items) with cooldowns full width below — optional. Minimum required: full stack below 1180px, 3-col at/above. If implementing the tablet 2-col, use "md:grid md:grid-cols-2 stack:flex" — but the simpler full-stack-below-1180 is acceptable and matches the canonical threshold. Choose full-stack-below-1180 for consistency with Row 2; document choice in SUMMARY.
    Row 2 (history | roshan+buildings 320px | map 420px), currently "flex gap-8 items-start":
      - Change to "flex flex-col gap-8 stack:flex-row stack:items-start".
      - History child: "min-w-0 stack:flex-1 flex flex-col" (full width stacked, flex-1 in row).
      - Roshan+buildings child: currently "shrink-0 w-[320px]" -> "w-full stack:w-[320px] stack:shrink-0 flex flex-col gap-8" (full width stacked, 320px capped in row).
      - Map child: currently "shrink-0" wrapping DotaMapView size=420 -> "w-full stack:w-auto stack:shrink-0 flex justify-center". DotaMapView is now fluid (Plan 05): keep size={420} (acts as max-width). When stacked, the map centers and caps at 420px via its own max-width; never forces horizontal scroll.
      - Keep the buildings.unavailable guard (placeholder behavior) and heroPositions mapping unchanged.
    Preserve the outer "mt-12 flex flex-col gap-12" wrapper and block ORDER (Row1 then Row2). Do NOT reorder blocks (10.3 IA locked).
  </action>
  <verify>
    <automated>cd client && npx vitest run && cd client && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "stack:flex-row" in MatchPage.tsx returns at least 2 (Row 1 + Row 2 both flip at 1180px).
    - grep -c "min-w-0" in MatchPage.tsx returns at least 3 (Row 1 children protected).
    - grep -c "flex flex-col gap-6" OR "flex flex-col gap-8" in MatchPage.tsx returns at least 1 (mobile-first base stack).
    - MatchPage still contains "game_state === 5" gate and "buildings.unavailable" guard (behavior preserved).
    - cd client && npx vitest run exits 0 (105 green).
    - cd client && npm run build exits 0 (tsc + vite build green).
  </acceptance_criteria>
  <done>Match in-game Row 1 + Row 2 reflow at the 1180px stack threshold with min-w-0 discipline and fluid map; block order + gates preserved; suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | CSS/markup-only restyle + responsive layout of read-only Match view |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-09 | T (Tampering) | MatchPage assembly | accept | No user input, no new data flow; polling/guards unchanged; presentational + layout only. |

No new attack surface: CSS/markup-only restyle + responsive layout; no user input, no new network calls; no auth/data flow change.
</threat_model>

<verification>
- cd client && npx vitest run -> 105 green.
- cd client && npm run build -> tsc + vite build green.
- No color-hex literals in MatchPage.tsx; no imperative hover.
- Phase 10.3 block order, polling-stop, building_state guard, in-game/draft gates preserved.
- Reflow correctness validated at the Wave 4 UAT checkpoint (Plan 08) — jsdom cannot test layout.
</verification>

<success_criteria>
MatchPage consumes Tactical Slate tokens with zero color-hex literals and CSS back-link hover; the in-game Row 1 and Row 2 grids stack to single column below 1180px with min-w-0 protection and a fluid map; 10.3 block order and all behavior preserved; suite + build green.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-07-SUMMARY.md` noting the stack-threshold approach used (named stack: variant), the tablet 2-col-vs-full-stack choice, the neutral-glow retint, and confirmation suite + build green. This SUMMARY is the handoff to the Wave 4 UAT checkpoint.
</output>
