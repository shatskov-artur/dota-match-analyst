---
phase: 10.4-visual-redesign-and-responsive
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - client/src/components/DraftSection.tsx
  - client/src/components/DraftTurnIndicator.tsx
  - client/src/components/DraftColumn.tsx
  - client/src/components/DraftPortrait.tsx
  - client/src/components/DraftTimeline.tsx
  - client/src/components/IntelTooltip.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "Draft section (turn indicator, columns, portraits, timeline) and IntelTooltip render in Tactical Slate tokens with no inline color hex"
    - "Draft IA preserved: CM 7.40 order, badge strips, ban X overlay, tooltip positioning all unchanged in behavior"
    - "Active-slot ember accents retoned to gold (primary) per slate palette; team labels team-colored"
  artifacts:
    - path: "client/src/components/DraftColumn.tsx"
      provides: "Tokenized draft column with gold active accent"
      contains: "var(--color-primary)"
    - path: "client/src/components/IntelTooltip.tsx"
      provides: "Tokenized floating intel card"
      contains: "var(--color-surface)"
  key_links:
    - from: "DraftPortrait/DraftTimeline tooltip trigger"
      to: "IntelTooltip"
      via: "showTooltip state (preserved)"
      pattern: "IntelTooltip"
---

<objective>
Migrate the Draft section components (sketch 004-B palette) and IntelTooltip to Tactical Slate tokens: DraftSection (wrapper — already hex-free), DraftTurnIndicator, DraftColumn, DraftPortrait, DraftTimeline, IntelTooltip. Retone legacy ember-red active accents to slate (gold primary for active-slot emphasis; team colors stay team-colored). Preserve every behavior: CM 7.40 ordering, badge strips, ban X overlay, ordinal badges, tooltip hover + flip positioning, hidden-profile handling.

Purpose: UI-01 (draft + tooltip consume tokens, no inline hex). Information architecture is LOCKED — restyle only.
Output: Tokenized draft components + tooltip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/sketches/themes/tactical-slate.css
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-01-SUMMARY.md

<interfaces>
Token utilities + runtime vars (Plan 01):
  text-radiant text-dire text-text text-text-muted text-text-dim  border-border  rounded-sm rounded-md  shadow-card
  var()-only: var(--color-radiant) var(--color-dire) var(--color-primary) var(--color-primary-soft) var(--glow-primary)
    var(--color-surface) var(--color-surface-2) var(--card-bg-grad) var(--color-border) var(--color-text) var(--color-text-muted) var(--color-text-dim) var(--shadow-card)
Legacy-ember to slate map (active-slot accents were ember red #b03030; retone to GOLD primary):
  #b03030 (active border) to var(--color-primary)
  rgba(176,48,48,0.25)/(0.10)/(0.8) (active glow) to var(--color-primary-soft)
  active bg #111111 to var(--color-surface-2)
Team/identity map:
  #4ade80 to var(--color-radiant)   ;   #ef4444 to var(--color-dire) (ban X stays dire red, the warning flag stays dire red)
Chrome map:
  placeholder bg #141414 to var(--color-surface)   ;   border #1e1e1e to var(--color-border)
  #303030 / #555 / #444 / #444444 to var(--color-text-dim)   ;   #888 / #888888 to var(--color-text-muted)
  #d8d8d8 to var(--color-text)
  divider #222 / #2a2a2a to var(--color-border)
  tooltip bg #111111 to var(--card-bg-grad); tooltip border #1a1a1a to var(--color-border)
  tooltip skeleton/counter placeholder #1a1a1a to var(--color-surface-2)
  tooltip shadow rgba(0,0,0,0.6) to var(--shadow-card)
ALLOWLISTED (keep, do NOT tokenize): winrateColor(...) JS calls (winrate-tier util, not chrome); decorative black scrims rgba(0,0,0,0.72) (badge bg), rgba(0,0,0,0.7)/(0.8) (drop-shadow/textShadow). These are intentional contrast scrims, not palette colors. The no-hex grep targets #hex only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retoken DraftTurnIndicator + DraftColumn + DraftSection</name>
  <read_first>
    - client/src/components/DraftTurnIndicator.tsx (hexes lines 58, 64, 72, 91)
    - client/src/components/DraftColumn.tsx (ember accents lines 39-48; label hex line 33; divider #222 line 81)
    - client/src/components/DraftSection.tsx (composition only — confirm hex-free)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md section "Component Inventory" (DraftSection: retoken to slate, preserve draft IA)
  </read_first>
  <action>
    DraftSection.tsx: confirm no color hex (composition only). No change expected; if any hex exists, tokenize. Do NOT alter render-path logic, prop forwarding, or class structure.
    DraftTurnIndicator.tsx:
      - "Draft in progress" label style color '#303030' to var(--color-text-dim).
      - phase sub-label style color '#555' to var(--color-text-dim).
      - active label color '#4ade80'/'#ef4444' to var(--color-radiant)/var(--color-dire) (team-colored, keep).
      - second phase sub-label '#555' to var(--color-text-dim).
      - Preserve the gameState !== 2 null gate, getPhaseName, em-dash label, tentative opacity.
    DraftColumn.tsx:
      - labelColor '#4ade80'/'#ef4444' to var(--color-radiant)/var(--color-dire).
      - borderLeft ember '2px solid #b03030' to '2px solid var(--color-primary)'; '2px dashed #b03030' to '2px dashed var(--color-primary)'; transparent stays.
      - boxShadow ember '-4px 0 12px rgba(176,48,48,0.25)' to '-4px 0 12px var(--color-primary-soft)'; the 0.10 variant likewise var(--color-primary-soft); none stays.
      - active background '#111111' to var(--color-surface-2).
      - picks/bans divider background '#222' to var(--color-border).
      - Preserve isActive/tentative logic, ordinal props, 5-pick/7-ban structure, prop forwarding.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -E for hex in DraftTurnIndicator.tsx returns 0 matches.
    - grep -E for hex in DraftColumn.tsx returns 0 matches.
    - grep -E for hex in DraftSection.tsx returns 0 matches.
    - grep -c "var(--color-primary)" in DraftColumn.tsx returns at least 1 (gold active accent).
    - DraftColumn contains var(--color-radiant) and var(--color-dire) for team labels.
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>DraftTurnIndicator/DraftColumn/DraftSection tokenized; ember active accents now gold primary; team labels team-colored; logic preserved; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Retoken DraftPortrait + DraftTimeline</name>
  <read_first>
    - client/src/components/DraftPortrait.tsx (placeholder #141414/#1e1e1e/#b03030 lines 63-64; ban X #ef4444 line 93; badge suffix #888888 line 126; ordinal #888 line 144)
    - client/src/components/DraftTimeline.tsx (labels #4ade80/#ef4444 lines 32,34; placeholder #141414/#1e1e1e lines 54-55; active ember rgba(176,48,48,0.8) line 55; ban X #ef4444 line 64; divider #2a2a2a line 97; step #444 line 101)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md section "Component Inventory" (preserve draft IA, badge strips)
  </read_first>
  <action>
    DraftPortrait.tsx:
      - Empty placeholder background '#141414' to var(--color-surface); active border '#b03030' to var(--color-primary); inactive border '#1e1e1e' to var(--color-border). Keep animate-pulse on active.
      - Ban X svg style color '#ef4444' to var(--color-dire). Keep opacity 0.75 and the rgba(0,0,0,0.7) drop-shadow scrim (allowlisted).
      - Badge strip: keep winrateColor(heroStats.win_rate) UNCHANGED. The "P" suffix color '#888888' to var(--color-text-muted). Keep rgba(0,0,0,0.72) scrim (allowlisted).
      - Ordinal badge color '#888' to var(--color-text-muted). Keep textShadow black scrim.
      - PRESERVE: outer wrapper relative-without-overflow-hidden (Pitfall 4), inner overflow-hidden, onMouseEnter/onMouseLeave tooltip triggers (state-based, NOT .style), IntelTooltip render, canShowTooltip logic.
    DraftTimeline.tsx:
      - Row labels 'R' color '#4ade80' to var(--color-radiant); 'D' '#ef4444' to var(--color-dire).
      - Portrait background '#141414' to var(--color-surface); active border rgba(176,48,48,0.8) to var(--color-primary); inactive '#1e1e1e' to var(--color-border).
      - Ban X color '#ef4444' to var(--color-dire) (keep black drop-shadow scrim).
      - Badge winrateColor UNCHANGED; keep rgba(0,0,0,0.72) scrim.
      - Phase divider background '#2a2a2a' to var(--color-border).
      - Step number color '#444' to var(--color-text-dim).
      - PRESERVE: PHASE_DIVIDER_BEFORE set, two-row chess layout, hoveredStep state, portraitRefs, IntelTooltip flip logic, spacer/portrait placement.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -E for hex in DraftPortrait.tsx returns 0 matches.
    - grep -E for hex in DraftTimeline.tsx returns 0 matches.
    - grep -c "var(--color-primary)" in DraftPortrait.tsx returns at least 1 (active slot accent).
    - DraftTimeline contains var(--color-radiant) and var(--color-dire) for row labels.
    - winrateColor( calls still present (unchanged) in both files.
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>DraftPortrait/DraftTimeline tokenized; placeholders/active accents/labels slated; winrateColor + tooltip + ban-X behavior preserved; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Retoken IntelTooltip</name>
  <read_first>
    - client/src/components/IntelTooltip.tsx (hexes lines 48, 53, 59, 74-78, 88, 108, 113, 130, 134, 144, 152, 186, 194, 201, 206)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md section "Component Inventory" (no IntelTooltip row but it inherits slate card styling: card-bg-grad, border, shadow-card)
  </read_first>
  <action>
    IntelTooltip.tsx — tokenize all inline color hexes; preserve positioning logic and copy:
      - Card container background '#111111' to var(--card-bg-grad); border '1px solid #1a1a1a' to '1px solid var(--color-border)'; boxShadow '0 4px 16px rgba(0,0,0,0.6)' to var(--shadow-card). borderRadius 4 to var(--radius-md).
      - Loading "Loading..." color '#444444' to var(--color-text-dim).
      - Hidden-profile stat line color '#888888' to var(--color-text-muted); normal stat line '#888888' to var(--color-text-muted). Keep the em-dash (U+2014) and "—%" copy EXACTLY (PLAYER-02).
      - Player name color '#d8d8d8' to var(--color-text).
      - Separators background '#1a1a1a' to var(--color-border).
      - "Counters" section labels color '#444444' to var(--color-text-dim).
      - Loading/counter placeholder squares + bars background '#1a1a1a' to var(--color-surface-2).
      - Counter hero name color '#d8d8d8' to var(--color-text).
      - Warning flag color '#ef4444' to var(--color-dire) (keep the warning glyph + spacing).
      - Known-player name color '#888888' to var(--color-text-muted); keep truncation logic.
      - PRESERVE: useLayoutEffect flip logic (rect.top >= 180), positionStyle, pointerEvents none, minWidth/maxWidth, hidden-profile null handling, counters map, all copy strings.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -E for hex in IntelTooltip.tsx returns 0 matches.
    - grep -c "var(--card-bg-grad)" in IntelTooltip.tsx returns at least 1.
    - grep -c "var(--color-surface-2)" in IntelTooltip.tsx returns at least 1 (placeholders).
    - IntelTooltip still contains useLayoutEffect and the flip threshold 180 (positioning preserved).
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>IntelTooltip fully tokenized (slate card, surface-2 placeholders, dire warning flag); flip positioning + hidden-profile copy preserved; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | CSS/markup-only restyle of read-only draft/intel views |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-07 | I (Information disclosure) | IntelTooltip hidden-profile path | accept | Hidden-profile rendering unchanged (em-dash, no stats); PLAYER-02 guard preserved, no new data exposed. |
| T-10.4-08 | T (Tampering) | Draft markup | accept | No user input, no new data flow; presentational change only. |

No new attack surface: CSS/markup-only restyle; no user input, no new network calls; no auth/data flow change.
</threat_model>

<verification>
- cd client && npx vitest run -> 105 green.
- No color-hex literals in any of the 6 draft/intel files (greps above); only allowlisted rgba black scrims + winrateColor() remain.
- Draft IA, tooltip flip, hidden-profile copy, ban-X, badge strips all preserved.
</verification>

<success_criteria>
All 6 draft/intel files consume Tactical Slate tokens with zero color-hex literals; ember-red active accents are now gold primary; team identity stays team-colored; winrateColor util, tooltip positioning, hidden-profile handling, badge strips, and CM 7.40 ordering preserved; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-06-SUMMARY.md` noting the ember-to-gold active-accent decision and the allowlisted black-scrim/winrateColor exceptions.
</output>
