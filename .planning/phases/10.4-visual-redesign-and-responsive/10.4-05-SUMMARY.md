---
phase: 10.4-visual-redesign-and-responsive
plan: 05
subsystem: frontend-match-row2
tags: [tailwind4, css-tokens, svg, historygraphs, roshan, buildings, map, vitest]
requires:
  - "Tactical Slate @theme token layer in client/src/index.css (Plan 01)"
  - "HistoryGraphs softened team consts (#6bcf8a/#e06a72) from Plan 01"
provides:
  - "DotaMapView: var() team-token SVG fills/strokes + fluid sizing (width 100%, max-width cap, aspect-ratio 1)"
  - "HistoryGraphs: slate chrome consts (var() tokens) with literal-softened team consts for the test; peak labels + Russian copy preserved"
  - "BuildingsSection: tier dots radiant/dire/border tokens (justify-center kept); RoshanBlock: gold-tinted loot, big mono timer (clamp + tick kept)"
affects: [match-page, row-2-visuals]
tech-stack:
  added: []
  patterns:
    - "SVG fill/stroke via runtime var() tokens for NON-tested attrs; literal softened hex for jsdom-tested fills (HistoryGraphs)"
    - "Fluid SVG map: drop fixed width/height attrs, keep viewBox, set style width:100% + maxWidth + aspectRatio (UI-03 no horizontal scroll)"
    - "Filled loot gold-tint via boxShadow inset 0 0 0 1px var(--color-primary-soft)"
key-files:
  created:
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-05-SUMMARY.md
  modified:
    - client/src/components/DotaMapView.tsx
    - client/src/components/HistoryGraphs.tsx
    - client/src/components/BuildingsSection.tsx
    - client/src/components/RoshanBlock.tsx
key-decisions:
  - "var()-vs-literal split: DotaMapView SVG (not under test) uses var(--color-radiant/dire); HistoryGraphs team consts stay LITERAL softened hex (#6bcf8a/#e06a72) because the test asserts fills and jsdom does not resolve CSS vars in SVG"
  - "HistoryGraphs Russian copy ('Накапливаем историю…', 'Историческая динамика') deliberately retained — restyle != recopy (RESEARCH A1)"
  - "10.3 decisions preserved: BuildingsSection justify-center, RoshanBlock max-w-[360px] mx-auto clamp + 1Hz anchor/resync tick"
requirements: [UI-01, UI-03]
duration: split (Task 1 worktree executor; Tasks 2-3 inline orchestrator)
completed: 2026-06-07
---

# Phase 10.4 Plan 05: Match Row-2 Visuals Retoken Summary

**Migrated the Match Row-2 visual components (sketch 004-B) to Tactical Slate: DotaMapView SVG now uses var() team tokens and is fluid (no horizontal scroll, UI-03); HistoryGraphs chrome consts are var() tokens with team consts kept literal-softened for the jsdom test; BuildingsSection tier dots and RoshanBlock loot/timer are tokenized with gold-tinted loot and a big mono timer — the deliberate Russian copy, the 10.3 building justify-center, and the roshan clamp + tick are all preserved; 105-test suite green.**

## Performance
- **Tasks:** 3 / 3 completed
- **Files modified:** 4

## Execution note
Task 1 (DotaMapView) was completed and committed by the parallel worktree executor (`4ffe187`, merged via `5369644`). Tasks 2 (HistoryGraphs) and 3 (BuildingsSection/RoshanBlock) were completed inline by the orchestrator (`c943c82`, `d8b27c8`) after the session permission environment blocked background-agent writes (per user decision "Finish inline, no subagents").

## What Was Built

### Task 1 — DotaMapView SVG + fluid sizing
- Dot/ancient/hero-ring fills+strokes → `var(--color-radiant)`/`var(--color-dire)`/`var(--color-border)`; backdrop → `var(--color-surface)`; labels → `var(--color-text-dim)`; dot shadows → `var(--color-radiant-soft)`/`var(--color-dire-soft)` drop-shadows.
- Fluid: dropped fixed `width/height={size}`, kept `viewBox`, set style `width:100%`, `maxWidth: size`, `aspectRatio: '1 / 1'` — map scales down on tablet/phone without forcing horizontal scroll. Blip r=8 and all coords preserved.

### Task 2 — HistoryGraphs panel retoken
- Chrome consts `PANEL_BG`→`var(--card-bg-grad)`, `PANEL_BORDER`/`ZERO_AXIS`/`GRID_LINE`→`var(--color-border)`, `SECONDARY_FG`→`var(--color-text-muted)`, `TERTIARY_FG`→`var(--color-text-dim)`; panel radius → `var(--radius-lg)`.
- Team consts kept LITERAL `#6bcf8a`/`#e06a72` (test-asserted). Soft fills, peak dots/labels, no-hover, and the Russian copy left unchanged (A1).

### Task 3 — BuildingsSection + RoshanBlock
- BuildingsSection: tier dots → `var(--color-radiant)`/`var(--color-dire)`/`var(--color-border)`; labels → `var(--color-text-dim)`; divider → `var(--color-border)`; `justify-center` (10.3) preserved.
- RoshanBlock: loot slots → `bg-surface-2`/`var(--color-border)`; filled loot gold-tint (`inset 0 0 0 1px var(--color-primary-soft)`); labels → `var(--color-text-dim)`; big respawn timer → `var(--color-text)` + `font-mono`; last-drop border → `var(--color-border)`. `max-w-[360px] mx-auto` clamp and 1Hz anchor/resync tick preserved.

## Verification

- `grep -E "#[0-9a-fA-F]{3,6}"` → **0** for DotaMapView, BuildingsSection, RoshanBlock.
- HistoryGraphs chrome hex (`#0f0f0f|#161616|#888888|#555555|#2a2a2a|#1a1a1a`) → **0**; team consts `#6bcf8a` + `#e06a72` → present; `var(--card-bg-grad)` present; Russian strings present; `onMouseMove` → 0.
- DotaMapView: `var(--color-radiant)`/`var(--color-dire)` + `maxWidth` + `aspectRatio` present. BuildingsSection: `justify-center` present. RoshanBlock: `max-w-[360px]`, `var(--color-primary-soft)`, `font-mono`, `setInterval` present.
- `cd client && npx vitest run` → **105 passed (15 files)** incl. HistoryGraphs 13 + RoshanBlock 6, exit 0.

Map/chart fluidity validated at the Wave 4 UAT checkpoint (Plan 08).

## Deviations from Plan

None to the planned changes. Process deviation: Tasks 2-3 completed inline by the orchestrator rather than the worktree executor due to the permission environment.

## Known Stubs

None — presentational restyle of existing wired data only.

## Self-Check: PASSED

- FOUND: all 4 Row-2 files modified, committed in 4ffe187 (T1), c943c82 (T2), d8b27c8 (T3)
- FOUND: .planning/phases/10.4-visual-redesign-and-responsive/10.4-05-SUMMARY.md
- Commits 4ffe187, c943c82, d8b27c8 present in git log.
