---
phase: 10.4-visual-redesign-and-responsive
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - client/src/components/HeroPlayerGrid.tsx
  - client/src/components/PlayerRow.tsx
  - client/src/components/SkeletonPlayerRow.tsx
  - client/src/components/ItemsBlock.tsx
  - client/src/components/CooldownsBlock.tsx
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "Row-1 dense panels (heroes/players, items, cooldowns) render in Tactical Slate tokens with no inline color hex"
    - "Row/item/cooldown hover uses CSS surface-2 (no imperative JS .style handlers)"
    - "Dense-data legibility floors preserved: portraits, 26x26 item slots, cooldown icons, 12px-min numerics, tabular-nums"
  artifacts:
    - path: "client/src/components/ItemsBlock.tsx"
      provides: "Tokenized item rows with CSS hover, gold-tinted filled slots"
      contains: "hover:bg-surface-2"
    - path: "client/src/components/CooldownsBlock.tsx"
      provides: "Tokenized cooldown rows, steel timer, CSS hover; min-h-0/overflow preserved"
      contains: "var(--color-accent)"
  key_links:
    - from: "ItemsBlock/PlayerRow/CooldownsBlock rows"
      to: "hover state"
      via: "hover:bg-surface-2 CSS"
      pattern: "hover:bg-surface-2"
---

<objective>
Migrate the Match Row-1 dense panels (sketch 004-B) to Tactical Slate tokens: HeroPlayerGrid + PlayerRow (hero portraits team-outlined, kda muted mono, nw gold mono), SkeletonPlayerRow, ItemsBlock (6x 26px slots, gold-tinted filled state, owner label truncates first), CooldownsBlock (ult icon team-outlined, steel mono timer). Replace the three imperative JS hover handlers (PlayerRow, ItemsBlock, CooldownsBlock) with CSS hover. Preserve the UI-03 legibility floors and the CooldownsBlock client-tick + overflow behavior.

Purpose: UI-01 (no inline hex on dense panels), UI-03 (dense data stays legible; floors honored).
Output: Tokenized Row-1 panels with CSS hover and preserved data glyphs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/sketches/themes/tactical-slate.css
@.claude/skills/sketch-findings-dota-stats/SKILL.md
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-01-SUMMARY.md

<interfaces>
Token utilities (Plan 01):
  text-text text-text-muted text-text-dim text-radiant text-dire text-gold text-accent
  bg-surface bg-surface-2  border-border  font-mono  tabular-nums  rounded-sm rounded-md
  var()-only: var(--color-radiant) var(--color-dire) var(--color-accent) var(--color-primary-soft) var(--color-gold)
Literal to token map for Row-1 panels:
  #4ade80 to var(--color-radiant) / text-radiant   ;   #ef4444 to var(--color-dire) / text-dire
  #e8e8e8 to text-text   ;   #888888 to text-text-muted   ;   #666666 / #555555 / #444444 to text-text-dim
  border #1e1e1e / #2a2a2a to border-border   ;   empty-slot bg #1a1a1a to bg-surface-2
  placeholder bg #141414 to bg-surface   ;   neutral-slot border #888866 to var(--color-primary) (gold)
  hover bg #0f0f0f (imperative) to CSS hover:bg-surface-2
  cooldown timer color #e8e8e8 -> keep value text-text BUT timer numerics are STEEL per UI-SPEC -> text-accent / var(--color-accent)
UI-03 legibility floors (do NOT shrink below): player portrait 48px stays (>=40x28 floor); item slots keep 32px here (>=26 floor, current 32 is fine); cooldown ult icon keep 32px; numerics >=12px; tabular-nums always.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retoken HeroPlayerGrid + PlayerRow + SkeletonPlayerRow (CSS hover, team-colored portraits, mono nw/kda)</name>
  <read_first>
    - client/src/components/HeroPlayerGrid.tsx (hexes at lines 26-41 ColHeaders #555555, 67 #4ade80, 75 #ef4444)
    - client/src/components/PlayerRow.tsx (imperative hover lines 48-49; hexes lines 47, 68, 74, 90, 94, 101, 107-111, 115, 121, 128, 135)
    - client/src/components/SkeletonPlayerRow.tsx (gradient hexes lines 5, 11, 19, 27)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md sections "Component Inventory" (HeroPlayerGrid/PlayerRow), "Dense-data legibility on phone", "Interaction Contract" (row hover)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md section "Pattern 3" (imperative to CSS hover)
  </read_first>
  <action>
    PlayerRow.tsx:
      - REMOVE the imperative onMouseEnter/onMouseLeave on the row (lines 48-49). Replace the row's inline style borderColor '#1e1e1e' + transition with className "border-border transition-colors duration-150 hover:bg-surface-2". Keep minHeight:52 inline (non-color) or move to className min-h-[52px].
      - KEEP the portrait-div onMouseEnter/onMouseLeave (lines 57-58) — those drive the IntelTooltip, NOT a style mutation; leave intact.
      - Portrait placeholder bg '#141414' to bg-surface (className) on the placeholder div. Add team-colored outline ring per UI-SPEC: ring color = radiant/dire. Use style ring via boxShadow 'inset 0 0 0 1.5px var(--color-radiant)' for team===0 else var(--color-dire) on the portrait img wrapper (the row exposes player.team: 0=radiant,1=dire). Keep opacity isDead logic.
      - Dead respawn label color '#585858' to text-text-dim.
      - Name color '#e8e8e8' to text-text; hero-name '#666666' to text-text-dim.
      - LVL '#888888' to text-text-muted.
      - K/D/A: kills/assists '#e8e8e8' to text-text; slashes '#444444' to text-text-dim; deaths '#ef4444' to var(--color-dire). Keep font-mono tabular-nums.
      - NW '#e8e8e8' -> gold per UI-SPEC (nw gold mono): style color 'var(--color-gold)'; add font-mono. Keep tabular-nums and 56px width.
      - GPM/XPM/LH-DN '#888888' to text-text-muted.
      - All numerics stay >= 12px (already 12px) — do NOT reduce.
    HeroPlayerGrid.tsx:
      - ColHeaders label color '#555555' to text-text-dim (all instances).
      - Radiant group label '#4ade80' to text-radiant; Dire group label '#ef4444' to text-dire.
    SkeletonPlayerRow.tsx:
      - borderColor '#1e1e1e' to border-border (className).
      - shimmer gradient stops #181818/#222222 to neutral surface tones: replace with linear-gradient using var(--color-surface) and var(--color-surface-2) (e.g. 'linear-gradient(90deg, var(--color-surface) 0%, var(--color-surface-2) 50%, var(--color-surface) 0%)'). The solid #181818 fallback to var(--color-surface). Keep the skshimmer keyframes.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/PlayerRow.tsx" returns 0 matches.
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/HeroPlayerGrid.tsx" returns 0 matches.
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/SkeletonPlayerRow.tsx" returns 0 matches.
    - "grep -c \"hover:bg-surface-2\" client/src/components/PlayerRow.tsx" returns at least 1.
    - PlayerRow row no longer has imperative onMouseEnter setting .style.background (grep for ".style.background" on the row returns 0; the portrait IntelTooltip handlers remain and use setShowTooltip, not .style).
    - HeroPlayerGrid contains text-radiant and text-dire for the group labels.
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>HeroPlayerGrid/PlayerRow/SkeletonPlayerRow tokenized; CSS row hover; team-ringed portraits; gold mono NW; legibility floors intact; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Retoken ItemsBlock + CooldownsBlock (CSS hover, gold-tinted slots, steel timer, overflow preserved)</name>
  <read_first>
    - client/src/components/ItemsBlock.tsx (imperative hover lines 101-102; hexes lines 43-44 empty slot, 37 neutral border #888866, 70 #555555, 77 rankColor, 128 placeholder #141414, 139 #e8e8e8)
    - client/src/components/CooldownsBlock.tsx (imperative hover lines 108-109; hexes lines 26 empty slot, 90 #555555, 103 #1e1e1e, 126 #e8e8e8, 128 #555555, 131 #4ade80; root overflow line 87)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md sections "Component Inventory" (ItemsBlock/CooldownsBlock), "Dense-data legibility on phone", "Typography" (cooldown timer 15px steel)
    - CLAUDE.md (do not regress CooldownsBlock client-tick / overflow behavior)
  </read_first>
  <action>
    ItemsBlock.tsx:
      - REMOVE imperative onMouseEnter/onMouseLeave on the item row (lines 101-102). Replace row inline borderColor '#1e1e1e' + transition with className "border-border transition-colors duration-150 hover:bg-surface-2". Keep minHeight/gap inline or move to className.
      - Section header '#555555' to text-text-dim.
      - rankColor JS const (#4ade80/#ef4444) to var(--color-radiant)/var(--color-dire). (Keeps team identity on rank number.)
      - Hero portrait placeholder bg '#141414' to bg-surface.
      - NW value '#e8e8e8' -> gold mono per UI-SPEC (nw gold): style color 'var(--color-gold)'; add fontFamily via className font-mono. Keep 56px width, tabular-nums.
      - Empty ItemSlot bg '#1a1a1a' to bg-surface-2; border '#2a2a2a' to border-border (use style borderColor 'var(--color-border)').
      - Neutral slot border '#888866' to var(--color-primary) (gold neutral border per UI-SPEC).
      - UI-SPEC: filled main slots get a gold-tint highlight. Add to the rendered img a subtle filled-state highlight: wrap or style with boxShadow 'inset 0 0 0 1px var(--color-primary-soft)' OR background var(--color-primary-soft) behind the icon. Keep 32px slot size (>=26 floor OK) and 4px gap.
      - UI-03: keep 6 slots, never wrap/reduce. The owner/rank+nw columns may shrink before slots; add min-w-0 to the flex row so slots are protected.
    CooldownsBlock.tsx:
      - REMOVE imperative onMouseEnter/onMouseLeave on the cooldown row (lines 108-109). Replace row inline borderColor '#1e1e1e' + transition with className "border-border transition-colors duration-150 hover:bg-surface-2".
      - PRESERVE root className "flex flex-col flex-1 min-h-0 overflow-y-auto" EXACTLY (10.2-03 overflow fix — do not regress).
      - PRESERVE the client-tick logic (sigRef/referenceRef/setInterval) EXACTLY — no behavior change.
      - Section header '#555555' to text-text-dim.
      - Empty UltSlot bg '#1a1a1a' to bg-surface-2; border '#2a2a2a' borderColor var(--color-border).
      - Hero placeholder '#141414' to bg-surface (style background var(--color-surface)).
      - Timer numerics: currently '#e8e8e8' with 's' suffix '#555555'. Per UI-SPEC the cooldown timer is STEEL 15px mono: timer value style color 'var(--color-accent)', fontSize 15, font-mono, tabular-nums; the 's' suffix to text-text-dim.
      - "ready" label '#4ade80' to var(--color-radiant) (ready = radiant green identity is fine; UI-SPEC team-color reserved — green here reads as "available"; keep var(--color-radiant)).
      - Ult icon: add team outline ring (radiant/dire) on the UltSlot per UI-SPEC "ult icon team-outlined" — boxShadow 'inset 0 0 0 1.5px var(--color-radiant)' for team radiant else dire (CooldownPlayer has team field). Keep 32px size (>=cooldown floor).
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/ItemsBlock.tsx" returns 0 matches.
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/CooldownsBlock.tsx" returns 0 matches.
    - "grep -cE \"onMouseEnter|onMouseLeave\" client/src/components/ItemsBlock.tsx" returns 0.
    - "grep -cE \"onMouseEnter|onMouseLeave\" client/src/components/CooldownsBlock.tsx" returns 0.
    - "grep -c \"min-h-0 overflow-y-auto\" client/src/components/CooldownsBlock.tsx" returns at least 1 (10.2-03 overflow preserved).
    - "grep -c \"var(--color-accent)\" client/src/components/CooldownsBlock.tsx" returns at least 1 (steel timer).
    - "grep -c \"hover:bg-surface-2\" client/src/components/ItemsBlock.tsx" returns at least 1.
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>ItemsBlock + CooldownsBlock tokenized; CSS hover; gold-tinted slots; steel mono timer; team-outlined icons; overflow + client-tick preserved; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | CSS/markup-only restyle of read-only dense panels |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-05 | T (Tampering) | Row-1 panel markup | accept | No user input, no new data flow; presentational change only. Image src URLs unchanged (Valve CDN). |

No new attack surface: CSS/markup-only restyle; no user input, no new network calls; no auth/data flow change.
</threat_model>

<verification>
- cd client && npx vitest run -> 105 green.
- No color-hex literals in the 5 Row-1 files (greps above).
- No imperative .style hover handlers remain on rows (portrait IntelTooltip handlers excluded — they use React state).
- CooldownsBlock min-h-0/overflow-y-auto and client-tick preserved.
- Phone legibility validated at Wave 4 UAT (Plan 07).
</verification>

<success_criteria>
All 5 Row-1 dense-panel files consume Tactical Slate tokens with zero color-hex literals; row/item/cooldown hover is CSS surface-2; portraits team-ringed; NW gold mono; cooldown timer steel mono; item filled-state gold-tinted; UI-03 legibility floors and CooldownsBlock overflow/tick behavior preserved; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-04-SUMMARY.md` confirming the no-hex grep, preserved overflow/tick behavior, and the team-ring portrait approach used.
</output>
