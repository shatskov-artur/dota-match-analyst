---
phase: 10.4-visual-redesign-and-responsive
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - client/src/components/DotaMapView.tsx
  - client/src/components/HistoryGraphs.tsx
  - client/src/components/BuildingsSection.tsx
  - client/src/components/RoshanBlock.tsx
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "Map, history charts, buildings, and roshan render in Tactical Slate tokens with no inline color hex"
    - "DotaMapView SVG fills/strokes use var() team tokens and the map is fluid (max-width 420, aspect-ratio 1) instead of fixed 420px"
    - "HistoryGraphs full panel retoned to slate (line+soft-fill+peak labels preserved, no hover); Russian copy unchanged"
    - "BuildingsSection tier dots use radiant/border tokens; RoshanBlock mono timer + gold-tinted loot slots; 10.3 clamp kept"
  artifacts:
    - path: "client/src/components/DotaMapView.tsx"
      provides: "Tokenized SVG map, fluid sizing"
      contains: "var(--color-radiant)"
    - path: "client/src/components/HistoryGraphs.tsx"
      provides: "Slate-toned charts, peak labels preserved"
      contains: "#6bcf8a"
  key_links:
    - from: "DotaMapView Dot fills"
      to: "team tokens"
      via: "var(--color-radiant)/var(--color-dire)"
      pattern: "var\\(--color-radiant\\)"
---

<objective>
Migrate the Match Row-2 visual components (sketch 004-B) to Tactical Slate: DotaMapView (SVG fills/strokes via var() tokens + fluid sizing per UI-03), HistoryGraphs (finish the panel/border/grid retokening started in Plan 01; keep softened team consts + line/soft-fill/peak-label treatment + Russian copy), BuildingsSection (tier-dot grid radiant/border), RoshanBlock (big mono timer, gold-tinted loot slots, keep 10.3 max-w clamp).

Purpose: UI-01 (no inline hex on Row-2 visuals), UI-03 (map becomes fluid so it never forces horizontal scroll).
Output: Tokenized, fluid Row-2 visual components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/sketches/themes/tactical-slate.css
@.claude/skills/sketch-findings-dota-stats/SKILL.md
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-01-SUMMARY.md

<interfaces>
Token utilities + runtime vars (Plan 01):
  text-text text-text-muted text-text-dim  border-border  font-mono  tabular-nums  rounded-md rounded-lg  shadow-card
  var()-only / runtime: var(--color-radiant) var(--color-dire) var(--color-radiant-soft) var(--color-dire-soft)
    var(--color-border) var(--color-surface) var(--color-surface-2) var(--color-primary) var(--color-primary-soft)
    var(--card-bg-grad) var(--color-text-muted) var(--color-text-dim)
SVG note: jsdom does NOT resolve CSS vars. For SVG fill/stroke that a TEST asserts (HistoryGraphs only), use LITERAL softened hex consts (#6bcf8a / #e06a72) — matching the Plan 01 decision. For SVG attrs NOT under test (DotaMapView), use var(--color-radiant)/var(--color-dire) strings.
Literal to token map:
  #4ade80 to var(--color-radiant) (untested SVG) OR #6bcf8a (HistoryGraphs tested consts)
  #ef4444 to var(--color-dire) (untested SVG) OR #e06a72 (HistoryGraphs tested consts)
  panel bg #0f0f0f to var(--card-bg-grad) (HistoryGraphs/skeleton panels)
  panel border #161616 / divider #1e1e1e to var(--color-border)
  secondary #888888 to var(--color-text-muted)  ;  tertiary #555555 / #303030 to var(--color-text-dim)
  zero-axis #2a2a2a / grid #1a1a1a to var(--color-border) (chart axes/grid)
  map backdrop #07100a -> keep (decorative map fallback, NOT a palette token) OR var(--color-surface); prefer var(--color-surface)
  map label fills #1e3020 / #301e1e to var(--color-text-dim)
  empty loot/item slot #1a1a1a to var(--color-surface-2); border #2a2a2a to var(--color-border)
  roshan timer #e8e8e8 to var(--color-text); roshan placeholder #141414 to var(--color-surface)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retoken DotaMapView SVG (var() team fills) + make map fluid (UI-03)</name>
  <read_first>
    - client/src/components/DotaMapView.tsx (Dot color logic lines 30-33; ancient strokes 91, 117; hero rings 152; backdrop 51; labels 161-162; size prop default 420)
    - client/src/components/RoshanBlock.test.tsx (confirm no DotaMapView color assertions exist — DotaMapView fills are NOT under test, so var() is safe)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md sections "Component Inventory" (DotaMapView), "Match page reflow" (Row 2 map fluid), "Dense-data legibility" (map blip 12px)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md section "SVG fill via runtime token"
  </read_first>
  <action>
    DotaMapView.tsx — SVG fills/strokes are NOT under test, so use var() strings:
      - Dot color: alive radiant '#4ade80'/dire '#ef4444' to 'var(--color-radiant)'/'var(--color-dire)'; dead '#1c1c1c' to 'var(--color-border)'.
      - Dot shadow rgba(74,222,128,0.6)/rgba(239,68,68,0.6) to drop-shadow using var(--color-radiant-soft)/var(--color-dire-soft) (e.g. '0 0 5px var(--color-radiant-soft)').
      - Ancient stroke radiant '#4ade80' to var(--color-radiant); dire '#ef4444' to var(--color-dire); dead '#1c1c1c' to var(--color-border).
      - Hero ring stroke '#4ade80'/'#ef4444' to var(--color-radiant)/var(--color-dire).
      - Backdrop rect fill '#07100a' to var(--color-surface).
      - RADIANT/DIRE text labels fill '#1e3020'/'#301e1e' to var(--color-text-dim).
      - Fluid sizing (UI-03): the component takes size default 420 and renders width/height={size}. To prevent horizontal overflow on tablet/phone, change the root svg to fluid: set style {{ width: '100%', maxWidth: size, aspectRatio: '1 / 1', borderRadius: 6 }} and DROP the fixed width={size}/height={size} attributes (keep viewBox 0 0 320 320 so all dot coords still scale). The size prop becomes the max-width cap. This is the UI-SPEC map fix: width:100%; max-width:420px; aspect-ratio:1.
      - Keep hero blip r=8 (12px-equivalent floor OK), all dot coordinates, clipPath logic, minimap image href unchanged.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/DotaMapView.tsx" returns 0 matches.
    - "grep -c \"var(--color-radiant)\" client/src/components/DotaMapView.tsx" returns at least 1 and "var(--color-dire)" at least 1.
    - "grep -c \"maxWidth\" client/src/components/DotaMapView.tsx" returns at least 1 and "aspectRatio" at least 1 (fluid sizing).
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>DotaMapView SVG uses var() team tokens; map is fluid (max-width + aspect-ratio); suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Finish HistoryGraphs panel retokening (slate consts, soft fills, peak labels preserved)</name>
  <read_first>
    - client/src/components/HistoryGraphs.tsx (consts lines 9-16 — RADIANT_GREEN/DIRE_RED already swapped to #6bcf8a/#e06a72 in Plan 01; PANEL_BG/PANEL_BORDER/SECONDARY_FG/TERTIARY_FG/ZERO_AXIS/GRID_LINE still old)
    - client/src/components/HistoryGraphs.test.tsx (asserts fill #6bcf8a/#e06a72, headline color, peak-text regex, Russian skeleton text, no-hover)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md section "Component Inventory" (HistoryGraphs: keep line+soft-fill+peak-label, full-width stretch)
    - .claude/skills/sketch-findings-dota-stats/SKILL.md (prefer passive: NO hover on charts; static peak labels stay)
  </read_first>
  <action>
    HistoryGraphs.tsx — retone the remaining panel/axis consts (the team color consts are already #6bcf8a/#e06a72 from Plan 01). Because the TEST asserts literal fills, keep team consts as LITERAL softened hex (do NOT switch to var()). For the non-team chrome consts, switch to var() runtime tokens (these are not asserted):
      - PANEL_BG '#0f0f0f' to 'var(--card-bg-grad)' (panel background gradient). NOTE: this const is used as `background` in panelStyle and wrapperStyle — a gradient works as `background`.
      - PANEL_BORDER '#161616' to 'var(--color-border)'.
      - SECONDARY_FG '#888888' to 'var(--color-text-muted)'.
      - TERTIARY_FG '#555555' to 'var(--color-text-dim)'.
      - ZERO_AXIS '#2a2a2a' to 'var(--color-border)'.
      - GRID_LINE '#1a1a1a' to 'var(--color-border)'.
      - Keep RADIANT_GREEN='#6bcf8a' and DIRE_RED='#e06a72' as LITERAL consts (test-asserted).
      - Keep fillOpacity 0.15 soft fills, peak dots r=3.5, peak labels, anchorFor edge logic, gridlines, NO hover. Do NOT add tooltips.
      - DO NOT change the Russian copy strings ("Накапливаем историю…", "Историческая динамика") — restyle != recopy (RESEARCH A1). Flag in SUMMARY that copy is intentionally left in Russian per A1.
      - Full-width stretch already via preserveAspectRatio="none" + width:100% — keep.
      - Panel radius: the panel uses borderRadius 4 — bump to var(--radius-md) (10px) via inline 'var(--radius-md)' to match the slate card radius, or keep — UI-SPEC cards use radius-lg; set panelStyle borderRadius to 'var(--radius-lg)'.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - "grep -cE \"#0f0f0f|#161616|#888888|#555555|#2a2a2a|#1a1a1a\" client/src/components/HistoryGraphs.tsx" returns 0 (chrome consts retoned to var()).
    - "grep -c \"#6bcf8a\" client/src/components/HistoryGraphs.tsx" returns at least 1 and "#e06a72" at least 1 (team consts stay literal for the test).
    - "grep -c \"var(--card-bg-grad)\" client/src/components/HistoryGraphs.tsx" returns at least 1.
    - HistoryGraphs still contains the Russian strings "Накапливаем историю" and "Историческая динамика" (copy unchanged).
    - No hover handlers added (grep for "onMouseMove" returns 0).
    - cd client && npx vitest run exits 0 (105 green; HistoryGraphs 13 tests included).
  </acceptance_criteria>
  <done>HistoryGraphs panel/axes retoned to slate var() tokens; team consts literal-softened for the test; peak labels + no-hover + Russian copy preserved; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Retoken BuildingsSection + RoshanBlock</name>
  <read_first>
    - client/src/components/BuildingsSection.tsx (standingColor #4ade80/#ef4444 line 13; dead #303030 line 19; labels #303030 lines 28, 36, 68; divider #1e1e1e line 53)
    - client/src/components/RoshanBlock.tsx (loot slot #1a1a1a/#2a2a2a line 26; labels #555555 lines 95,104,127; timer #e8e8e8 line 113; border #1e1e1e line 126; keep max-w-[360px] clamp line 92)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md sections "Component Inventory" (BuildingsSection alive radiant / dead border, keep justify-center; RoshanBlock big mono timer, loot gold-tinted, keep 10.3 clamp)
  </read_first>
  <action>
    BuildingsSection.tsx:
      - BuildingDot standingColor radiant '#4ade80' to var(--color-radiant); dire '#ef4444' to var(--color-dire); dead '#303030' to var(--color-border). Keep the standing/opacity logic.
      - Section label '#303030' to var(--color-text-dim) (className text-text-dim or style).
      - Lane labels '#303030' to var(--color-text-dim).
      - Center divider '#1e1e1e' to var(--color-border).
      - PRESERVE "justify-center" on the columns row (10.3 decision) and the 9px tier-dot size (w-2 h-2 — fixed glyph).
    RoshanBlock.tsx:
      - LootIcon empty bg '#1a1a1a' to var(--color-surface-2); border '#2a2a2a' to var(--color-border).
      - Loot icons filled state: UI-SPEC loot slots gold-tinted — add a subtle gold tint behind filled loot icons: style background 'var(--color-primary-soft)' on the img wrapper OR boxShadow 'inset 0 0 0 1px var(--color-primary-soft)'. Keep the icon sizes (32/28/20) — fixed glyphs.
      - "Roshan #N" / "Respawn" / "Last Drop" labels '#555555' to var(--color-text-dim).
      - Respawn timer '#e8e8e8' to var(--color-text); keep fontSize 28 (big mono), add fontFamily mono via className font-mono, keep tabular-nums.
      - Last-drop border '#1e1e1e' to var(--color-border).
      - PRESERVE the max-w-[360px] mx-auto clamp (10.3) and the 1Hz tick + anchor/resync logic EXACTLY.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/BuildingsSection.tsx" returns 0 matches.
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/RoshanBlock.tsx" returns 0 matches.
    - "grep -c \"justify-center\" client/src/components/BuildingsSection.tsx" returns at least 1 (10.3 preserved).
    - "grep -c \"max-w-\\[360px\\]\" client/src/components/RoshanBlock.tsx" returns at least 1 (10.3 clamp preserved).
    - "grep -c \"var(--color-radiant)\" client/src/components/BuildingsSection.tsx" returns at least 1.
    - RoshanBlock contains "var(--color-primary-soft)" (gold-tinted loot) and font-mono on the timer.
    - cd client && npx vitest run exits 0 (105 green; RoshanBlock 6 tests included).
  </acceptance_criteria>
  <done>BuildingsSection tier dots + labels tokenized (justify-center kept); RoshanBlock loot/timer/labels tokenized (clamp + tick kept); suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | CSS/SVG-only restyle of read-only Row-2 visuals |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-06 | T (Tampering) | Row-2 SVG/markup | accept | No user input, no new data flow; presentational change only. minimap.jpg/portrait/loot URLs unchanged. |

No new attack surface: CSS/SVG-only restyle; no user input, no new network calls; no auth/data flow change.
</threat_model>

<verification>
- cd client && npx vitest run -> 105 green (HistoryGraphs 13 + RoshanBlock 6 included).
- No color-hex literals in DotaMapView/BuildingsSection/RoshanBlock; HistoryGraphs only retains the two literal-softened team consts (test-required).
- Map is fluid; BuildingsSection justify-center + RoshanBlock max-w clamp preserved; RoshanBlock tick logic intact.
- Map/chart fluidity validated at Wave 4 UAT (Plan 07/08).
</verification>

<success_criteria>
DotaMapView, HistoryGraphs, BuildingsSection, RoshanBlock consume Tactical Slate tokens with zero color-hex literals except HistoryGraphs' two test-required softened team consts; map is fluid (max-width + aspect-ratio); charts keep line+soft-fill+peak-labels+no-hover and Russian copy; 10.3 building justify-center and roshan clamp preserved; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-05-SUMMARY.md` noting the var()-vs-literal split (DotaMapView var(), HistoryGraphs literal team consts), the map fluid fix, and the deliberate Russian-copy retention (A1).
</output>
