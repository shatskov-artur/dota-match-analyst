---
phase: 10.4-visual-redesign-and-responsive
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - client/src/pages/HomePage.tsx
  - client/src/components/LeagueAccordion.tsx
  - client/src/components/MatchRow.tsx
  - client/src/components/StatusTag.tsx
  - client/src/components/SkeletonRow.tsx
  - client/src/components/ErrorBanner.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Home page shell, header band, league panels, and match rows render in Tactical Slate tokens with no inline hex"
    - "Match-row hover shows a gold left rail + surface-2 bg via CSS (no imperative JS .style)"
    - "Home reflows cleanly: header live indicator never overflows, league panel goes full-width with phone gutters, match row collapses to 2 internal rows on phone with truncating team names"
  artifacts:
    - path: "client/src/pages/HomePage.tsx"
      provides: "Home shell + header + states retokened + responsive gutters"
      contains: "bg-bg"
    - path: "client/src/components/MatchRow.tsx"
      provides: "Token row, CSS gold hover rail, phone 2-row collapse"
      contains: "group-hover:bg-primary"
  key_links:
    - from: "MatchRow group parent"
      to: "left accent rail"
      via: "group-hover:bg-primary"
      pattern: "group-hover:bg-primary"
---

<objective>
Migrate the Home screen (sketch 003-B) to Tactical Slate tokens and make it responsive across the 4 widths. Covers HomePage shell + header + empty/error states, LeagueAccordion, MatchRow (incl. the imperative-hover → CSS-hover migration and the phone 2-row collapse), StatusTag, SkeletonRow, and ErrorBanner (cosmetic retoken + standardized copy).

Purpose: UI-01 (Home consumes tokens, no inline hex), UI-02/UI-03 (Home reflow at 375/768/1280/1536). Home's focal point is the league-accordion match-row list — keep it scannable.
Output: Fully tokenized, responsive Home.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/sketches/themes/tactical-slate.css
@.claude/skills/sketch-findings-dota-stats/SKILL.md
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-01-SUMMARY.md

<interfaces>
<!-- Token utilities available after Plan 01 (Tailwind 4 @theme auto-generates these). -->
bg-bg bg-bg-elev bg-surface bg-surface-2  text-text text-text-muted text-text-dim
text-primary text-accent text-radiant text-dire text-gold  border-border border-border-glow
rounded-sm rounded-md rounded-lg rounded-full  shadow-card  font-sans font-mono  tracking-label
var()-only (use in style={{}}): var(--card-bg-grad) var(--color-primary-soft) var(--color-dire-soft) var(--glow-primary)
Stack breakpoint variants: stack: (min-width:1180px) / max-stack: (max-width:1179px)
<!-- Literal→token map for Home files (the hex/utility shapes found in these files): -->
  bg-gray-950 → bg-bg          text-white → text-text (headings: text-text or pure #fff via Display only)
  text-green-400 → text-primary (brand wordmark is gold per UI-SPEC, not radiant green)
  text-gray-400 → text-text-muted   text-gray-800/border-gray-800 → border-border
  #d8d8d8 → text-text    #b8b8b8 → text-text-muted    #1a1a1a/#1e1e1e/#141414 → border-border
  #303030/#383838/#424242/#282828 → text-text-dim   #ffffff → text-text (or keep #fff only on Display h1)
  StatusTag Live dot/text → var(--color-dire) (Live = Dire-red tinted)
  StatusTag Draft dot/text → var(--color-primary) (Draft = gold tinted)
  ErrorBanner bg-red-950/border-red-800/text-red-300 → var(--color-danger) styling (border + text), bg var(--color-dire-soft)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retoken + responsive HomePage shell, header, states, StatusTag, SkeletonRow, ErrorBanner</name>
  <read_first>
    - client/src/pages/HomePage.tsx
    - client/src/components/StatusTag.tsx
    - client/src/components/SkeletonRow.tsx
    - client/src/components/ErrorBanner.tsx
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md §"Home page reflow", §"Copywriting Contract", §"Color"
    - .planning/sketches/themes/tactical-slate.css
  </read_first>
  <action>
    HomePage.tsx:
      - Root `min-h-screen bg-gray-950 text-white` → `min-h-screen bg-bg text-text font-sans`.
      - Header `border-b border-gray-800` → `border-b border-border`; add responsive gutters `px-4 md:px-6 py-6` (16px phone → 24px tablet+) and center content with `max-w-[1180px] mx-auto`.
      - Brand `h1 text-green-400 text-2xl font-bold` → gold wordmark `text-primary text-2xl font-semibold` (UI-SPEC: logo/wordmark gold). Keep "Dota 2 Match Analyst".
      - Add the live indicator per Copywriting Contract: render `"{N} live now"` (N = total matches across grouped) at Label size `text-[11px] uppercase tracking-label text-text-dim` with a pulsing dot (`<span class="w-[5px] h-[5px] rounded-full bg-radiant animate-pulse" />`). On phone it must not overflow: wrap below brand using `flex-wrap` on the header. Keep the existing `lastUpdatedLabel` span retokened to `text-text-dim`.
      - Empty state: keep exact UI-SPEC copy. Heading "No live matches right now" → `text-text text-xl font-semibold`; body → replace current text with the UI-SPEC body "Pro tournament games appear here as they go live. This page refreshes every 30 seconds." styled `text-text-muted text-sm`. Gutters `px-4 md:px-6`.
      - `main` content wrapper: `max-w-[1180px] mx-auto px-4 md:px-6`.
    StatusTag.tsx — replace the hardcoded `styleMap` hexes with token-driven values. Map: Live → dot+text `var(--color-dire)`; Draft → `var(--color-primary)`; Strategy → `var(--color-accent)`; Starting → `var(--color-radiant)`; Waiting/Break/Post-game/Unknown → `var(--color-text-dim)`. Replace `boxShadow` glow literals with `var(--glow-primary)`/`var(--glow-accent)` where a glow applies (gold for Draft, steel for Strategy) or `none`. Keep pulse behavior and the `0.18em`/uppercase Label styling, but switch the dim color refs to `var(--color-text-dim)`.
    SkeletonRow.tsx — `bg-gray-800` → `bg-surface-2`; `border-gray-800` → `border-border`. Keep animate-pulse.
    ErrorBanner.tsx — `bg-red-950 border border-red-800 text-red-300` → `style={{ background: 'var(--color-dire-soft)', borderColor: 'var(--color-danger)', color: 'var(--color-danger)' }}` with `className="p-4 border rounded-md text-sm"`. Replace copy with UI-SPEC error copy: heading "Couldn't load live matches." + body "We'll keep retrying automatically. Check your connection if this persists."
    Do NOT add error-handling BEHAVIOR (Phase 11) — cosmetic + copy only.
  </action>
  <verify>
    <automated>cd client && npx vitest run 2>&1 | tail -4 && node -e "const fs=require('fs');for(const f of ['src/pages/HomePage.tsx','src/components/StatusTag.tsx','src/components/SkeletonRow.tsx','src/components/ErrorBanner.tsx']){const c=fs.readFileSync(f,'utf8');const m=c.match(/#[0-9a-fA-F]{3,6}/g)||[];if(m.length){console.error('HEX in',f,m);process.exit(1)}}console.log('no color hex in Home shell files')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#[0-9a-fA-F]{3,6}" client/src/pages/HomePage.tsx` returns 0 matches.
    - `grep -E "#[0-9a-fA-F]{3,6}" client/src/components/StatusTag.tsx` returns 0 matches.
    - `grep -c "bg-gray-950" client/src/pages/HomePage.tsx` returns 0; `grep -c "text-green-400" client/src/pages/HomePage.tsx` returns 0.
    - HomePage contains `live now` and `animate-pulse`; ErrorBanner contains `Couldn't load live matches.` and `var(--color-danger)`.
    - `cd client && npx vitest run` exits 0 (105 green).
  </acceptance_criteria>
  <done>Home shell, header w/ live indicator, states, StatusTag, SkeletonRow, ErrorBanner fully tokenized + responsive; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Retoken LeagueAccordion + MatchRow with CSS hover rail and phone 2-row collapse</name>
  <read_first>
    - client/src/components/MatchRow.tsx (imperative onMouseEnter/onMouseLeave at lines 24-29; group-hover rail at line 33)
    - client/src/components/LeagueAccordion.tsx (inline style hexes at lines 38, 48, 58, 64, 72, 83)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md §"Home page reflow" (match row collapse), §"Interaction Contract" (match-row hover), §"Color" (gold rail)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md §"Pattern 3" (imperative→CSS hover), §"Pitfall 5"
  </read_first>
  <action>
    MatchRow.tsx:
      - REMOVE the imperative `onMouseEnter`/`onMouseLeave` handlers (lines 24-29) and the inline `style={{ transition... }}` on the Link. Replace with utilities: add `transition-colors duration-150 hover:bg-surface-2` to the Link className. (UI-SPEC says 140ms; use `duration-150`.)
      - The left accent rail (currently `w-[2px] group-hover:bg-[#b03030]`): change to a 3px GOLD rail per UI-SPEC: `absolute left-0 top-0 w-[3px] h-full bg-transparent transition-colors duration-150 group-hover:bg-primary`. Remove the inline transition style (use utility). NO glow on the rail (Tactical Slate override: box-shadow none).
      - Border: `border-[#1a1a1a]` → `border-border`. Team names `text-[#d8d8d8]` → `text-text`; the "vs" `text-[#303030]` → `text-text-dim`. Keep `truncate` + `min-w-0` on team-name spans (mandatory anti-overflow).
      - Right meta cluster hexes: `text-[#424242]` → `text-text-dim`, `text-[#303030]` → `text-text-dim`, `text-[#383838]` → `text-text-dim`. Keep `tabular-nums font-mono`.
      - Phone 2-row collapse (UI-SPEC): the row is currently `flex items-center gap-6`. Make it stack on phone: `flex flex-col gap-2 sm:flex-row sm:items-center sm:gap-6`. The team-name span stays full width on phone (Row A); the right meta cluster (`flex items-center gap-5`) wraps to Row B with `flex-wrap`. Keep team logos/names truncating; never clip names.
    LeagueAccordion.tsx — replace all inline-style hexes with tokens:
      - "Tournament" label `style={{ color: '#ffffff' }}` → `text-text` via className (drop inline style); keep uppercase tracking.
      - divider `style={{ background: '#1e1e1e' }}` → `bg-border` via className.
      - league name `style={{ color: '#b8b8b8', transition... }}` → `text-text-muted transition-colors duration-150` (className), drop inline color; keep hover affordance subtle (no color change required, but if kept use `group-hover:text-text`).
      - match count `style={{ color: '#303030' }}` → `text-text-dim`.
      - chevron `text-[#282828]` → `text-text-dim`; keep rotate transform inline (transform is not a color).
      - thin separators `style={{ background: '#141414' }}` and `style={{ background: '#1e1e1e' }}` → `bg-border`.
      - button `style={{ background: 'transparent' }}` is fine (no hex) — keep or drop. Add `min-h-[44px]` to the header button for phone tap target (UI-SPEC). Gutters: keep `px-8` but make responsive `px-4 md:px-8`.
  </action>
  <verify>
    <automated>cd client && npx vitest run 2>&1 | tail -4 && node -e "const fs=require('fs');for(const f of ['src/components/MatchRow.tsx','src/components/LeagueAccordion.tsx']){const c=fs.readFileSync(f,'utf8');const m=c.match(/#[0-9a-fA-F]{3,6}/g)||[];const bad=m.filter(x=>!['#fff'].includes(x.toLowerCase()));if(bad.length){console.error('HEX in',f,bad);process.exit(1)}if(/onMouseEnter|onMouseLeave|\.style\./.test(c)){console.error('imperative hover in',f);process.exit(1)}}console.log('Home rows clean')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "#[0-9a-fA-F]{3,6}" client/src/components/MatchRow.tsx` returns 0 matches (no `#b03030`, `#d8d8d8`, `#1a1a1a` etc.).
    - `grep -E "#[0-9a-fA-F]{3,6}" client/src/components/LeagueAccordion.tsx` returns 0 matches.
    - `grep -cE "onMouseEnter|onMouseLeave|\.style\." client/src/components/MatchRow.tsx` returns 0 (imperative hover removed).
    - MatchRow contains `group-hover:bg-primary` and `w-[3px]` (gold 3px rail) and `hover:bg-surface-2`.
    - MatchRow contains `sm:flex-row` (phone-first stack) and `min-w-0` + `truncate` on team names.
    - `cd client && npx vitest run` exits 0 (105 green).
  </acceptance_criteria>
  <done>LeagueAccordion + MatchRow fully tokenized, CSS gold hover rail, phone 2-row collapse, 44px tap targets; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | CSS/markup-only restyle of read-only analytics views |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-03 | T (Tampering) | HomePage/MatchRow markup | accept | No user input, no new data flow; pure presentational change. |

No new attack surface: CSS/markup-only restyle; no user input, no new network calls; no auth/data flow change.
</threat_model>

<verification>
- `cd client && npx vitest run` → 105 green.
- No color hex literals in any of the 6 Home files (automated greps above).
- No imperative hover handlers in MatchRow.
- Manual reflow validation is deferred to the Wave 4 UAT checkpoint (Plan 07) — jsdom cannot test layout.
</verification>

<success_criteria>
All 6 Home files consume Tactical Slate tokens with zero color-hex literals; MatchRow uses a CSS gold hover rail and collapses to 2 internal rows on phone with truncating names; header live indicator never overflows; ErrorBanner uses standardized copy + danger token; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-02-SUMMARY.md` noting any Home-specific reflow utilities used and confirming the no-hex grep + green suite.
</output>
