---
phase: 10.4-visual-redesign-and-responsive
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - client/src/components/ScoreHeader.tsx
  - client/src/components/WinProbBar.tsx
autonomous: true
requirements: [UI-01, UI-02]
must_haves:
  truths:
    - "ScoreHeader renders in Tactical Slate tokens: team tags team-colored, 56px mono kill scores responsive to 44/40px, roshan pill steel, gold-diff gold"
    - "WinProbBar track + R/D fills use team tokens, source labels mono, with no inline color hex"
    - "ScoreHeader stacks to 3 rows on phone with kill score at 40px; center meta wraps without overflow"
  artifacts:
    - path: "client/src/components/ScoreHeader.tsx"
      provides: "Tokenized 3-col scorebar with responsive kill scores + phone stack"
      contains: "var(--color-radiant)"
    - path: "client/src/components/WinProbBar.tsx"
      provides: "Tokenized three-bar panel"
      contains: "var(--color-radiant)"
  key_links:
    - from: "ScoreHeader kill score"
      to: "responsive type scale"
      via: "text-40 md-44 lg-56"
      pattern: "lg:text"
---

<objective>
Migrate the Match score region (sketch 004-B) to Tactical Slate tokens and responsive sizing: ScoreHeader (the Match-page primary focal point — 56px mono kill scores) and WinProbBar (three-bar win-prob panel). Both keep existing behavior (self-gating, formatGoldDiff, tabular-nums) and gain responsive kill-score scaling + phone stacking.

Purpose: UI-01 (no inline hex), UI-02 (score region reflows at the 4 widths).
Output: Tokenized, responsive ScoreHeader + WinProbBar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md
@.planning/sketches/themes/tactical-slate.css
@.planning/phases/10.4-visual-redesign-and-responsive/10.4-01-SUMMARY.md

<interfaces>
Token utilities (Plan 01):
  text-text text-text-muted text-text-dim text-radiant text-dire text-gold text-accent
  border-border  font-mono  tabular-nums
  var()-only: var(--color-radiant) var(--color-dire) var(--color-dire-soft) var(--color-accent) var(--color-accent-soft) var(--color-gold) var(--glow-accent)
Literal to token map for ScoreHeader/WinProbBar:
  #4ade80 to var(--color-radiant)   ;   #ef4444 to var(--color-dire)
  #ffffff (kill score) to text-text
  #666666 / #555555 (series/delay) to text-text-dim   ;   #888888 (game clock) to text-text-muted
  roshan pill #f97316 + rgba(249,115,22,*) to color var(--color-accent), bg var(--color-accent-soft), borderColor var(--color-accent)
  WinProbBar track #0f0f0f to var(--color-dire-soft) ; gradient #4ade80 / #ef4444 to var(--color-radiant) / var(--color-dire)
  borderBottom #1a1a1a to border-border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retoken + responsive ScoreHeader (kill scores 56/44/40, phone 3-row stack)</name>
  <read_first>
    - client/src/components/ScoreHeader.tsx (inline-style hexes at lines 51, 57, 63, 69, 81, 89, 96, 103, 112, 118, 124)
    - client/src/utils/formatGoldDiff.ts (goldDiff.color is JS-computed — confirm what it returns before overriding)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md sections "Match page reflow" (score header), "Typography" (Kill score 56/44/40; gold-diff gold), "Color" (steel roshan pill)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-RESEARCH.md section "Responsive ScoreHeader kill score"
  </read_first>
  <action>
    Edit ScoreHeader.tsx — replace all inline-style color hexes with tokens and add responsive sizing:
      - Score-row style borderBottom '1px solid #1a1a1a' becomes className "border-b border-border" (drop inline style).
      - Radiant team tag style color '#4ade80' becomes style color 'var(--color-radiant)'; Dire tag '#ef4444' becomes 'var(--color-dire)'. Use tracking-[0.16em] (UI-SPEC status-tag tracking).
      - Kill scores (both sides): currently text-[52px] with style color '#ffffff'. Change className to "text-[40px] md:text-[44px] lg:text-[56px] font-mono font-extrabold tabular-nums leading-none text-text" and DROP the inline white style. (UI-SPEC: phone 40 to tablet 44 to laptop+ 56; mono display weight 800 = font-extrabold.)
      - Series score style color '#666666' becomes text-text-dim className (drop inline).
      - Game clock style color '#888888' becomes text-text-muted className.
      - Roshan pill: replace style color '#f97316', background 'rgba(249,115,22,0.1)', border '1px solid rgba(249,115,22,0.2)' with style color 'var(--color-accent)', background 'var(--color-accent-soft)', borderColor 'var(--color-accent)' and add "border" to className. (UI-SPEC: Roshan timer pill steel.)
      - Gold-diff value style color goldDiff.color: OVERRIDE to style color 'var(--color-gold)' (UI-SPEC: gold-diff numeric reserved for --color-gold). Keep goldDiff.text. Do NOT edit formatGoldDiff.ts (out of file scope) — override only at this consumer.
      - Delay disclosure style color '#555555' becomes text-text-dim; render at Label size className "text-[11px] uppercase tracking-label" (folds old 10px into the 11px Label step). Keep delayLabel logic.
      - Phone 3-row stack (UI-SPEC): outer score row "flex items-center justify-between" becomes "flex flex-col gap-4 md:flex-row md:items-center md:justify-between". Dire side "items-end" becomes "items-start md:items-end". Center meta keeps its vertical flex; add flex-wrap to any horizontal sub-cluster so it never overflows. Add min-w-0 to the team-tag columns.
    Preserve ALL logic: NW reduction, getStatusLabel, formatDuration, StatusTag usage, tabular-nums. No behavior regression.
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/ScoreHeader.tsx" returns 0 matches.
    - "grep -c \"lg:text-\\[56px\\]\" client/src/components/ScoreHeader.tsx" returns at least 1 (responsive kill score present).
    - "grep -c \"var(--color-radiant)\" client/src/components/ScoreHeader.tsx" returns at least 1 and "var(--color-dire)" at least 1.
    - "grep -c \"var(--color-accent)\" client/src/components/ScoreHeader.tsx" returns at least 1 (steel roshan pill).
    - "grep -c \"md:flex-row\" client/src/components/ScoreHeader.tsx" returns at least 1 (phone stack).
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>ScoreHeader fully tokenized, responsive kill scores 56/44/40, steel roshan pill, gold-diff gold, phone 3-row stack; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Retoken WinProbBar three-bar panel</name>
  <read_first>
    - client/src/components/WinProbBar.tsx (hexes at lines 28, 35, 50-52, 59, 86)
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-UI-SPEC.md section "Match page reflow" (win-prob bars), "Color" (track dire-soft bg, fills team-colored)
  </read_first>
  <action>
    Edit WinProbBar.tsx — replace inline color hexes with tokens; keep all gating/logic:
      - Source label style color '#888888' becomes text-text-muted (className); keep minWidth/textAlign inline (non-color).
      - Radiant percent style color '#4ade80' becomes 'var(--color-radiant)'; Dire percent '#ef4444' becomes 'var(--color-dire)'.
      - Gradient bar: backgroundColor '#0f0f0f' becomes 'var(--color-dire-soft)' (track bg); background linear-gradient(to right, #4ade80 X%, #ef4444 X%) becomes linear-gradient(to right, var(--color-radiant) X%, var(--color-dire) X%). Keep height/borderRadius/transition inline (non-color). Keep prefers-reduced-motion logic.
      - Panel borderBottom '1px solid #1a1a1a' becomes className "border-b border-border" (drop inline).
      - Keep mono/tabular-nums on the percent values (add font-mono to the percent spans per UI-SPEC "mono values" if not present).
      - Keep self-gate (gameState !== 5 or duration <= 300 returns null) and the Stratz-null conditional EXACTLY.
    UI-02 note: the bars are already flex rows; ensure the track has a min width so it never collapses on 320px — add min-w-[120px] to the flex-1 bar div (UI-SPEC: keep track >= 120px on narrow phones).
  </action>
  <verify>
    <automated>cd client && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - "grep -E \"#[0-9a-fA-F]{3,6}\" client/src/components/WinProbBar.tsx" returns 0 matches.
    - "grep -c \"var(--color-radiant)\" client/src/components/WinProbBar.tsx" returns at least 1 and "var(--color-dire)" at least 1.
    - "grep -c \"var(--color-dire-soft)\" client/src/components/WinProbBar.tsx" returns at least 1 (track bg).
    - "grep -c \"min-w-\\[120px\\]\" client/src/components/WinProbBar.tsx" returns at least 1.
    - WinProbBar still returns null when gameState !== 5 (self-gate intact — grep for "gameState !== 5").
    - cd client && npx vitest run exits 0 (105 green).
  </acceptance_criteria>
  <done>WinProbBar tokenized: dire-soft track, team-colored fills, mono values, 120px min track; self-gate preserved; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | CSS/markup-only restyle of read-only match views |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.4-04 | T (Tampering) | ScoreHeader/WinProbBar markup | accept | No user input, no new data flow; presentational change only. |

No new attack surface: CSS/markup-only restyle; no user input, no new network calls; no auth/data flow change.
</threat_model>

<verification>
- cd client && npx vitest run -> 105 green.
- No color-hex literals in ScoreHeader.tsx or WinProbBar.tsx (greps above).
- ScoreHeader self-gating and WinProbBar self-gate logic unchanged.
- Responsive reflow validated at the Wave 4 UAT checkpoint (Plan 07) — jsdom cannot test layout.
</verification>

<success_criteria>
ScoreHeader + WinProbBar consume Tactical Slate tokens with zero color-hex literals; kill scores scale 56/44/40 and stack to 3 rows on phone; WinProbBar track is dire-soft with team-colored fills and a 120px min track; all existing gating logic preserved; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/10.4-visual-redesign-and-responsive/10.4-03-SUMMARY.md` noting the gold-diff override decision and confirming no-hex grep + green suite.
</output>
