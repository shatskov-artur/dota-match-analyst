---
phase: 10.4-visual-redesign-and-responsive
plan: 04
subsystem: frontend-match-row1
tags: [tailwind4, css-tokens, dense-panels, items, cooldowns, vitest]
requires:
  - "Tactical Slate @theme token layer in client/src/index.css (Plan 01)"
provides:
  - "Tokenized Row-1 dense panels: HeroPlayerGrid + PlayerRow (team-ringed portraits, gold mono NW), SkeletonPlayerRow"
  - "ItemsBlock: CSS hover, gold-tinted filled slots, gold neutral border, min-w-0 slot protection"
  - "CooldownsBlock: steel 15px mono timer, team-outlined ult icon, CSS hover; overflow + client-tick preserved"
affects: [match-page, row-1-panels]
tech-stack:
  added: []
  patterns:
    - "Imperative JS row hover (.style mutation) -> CSS hover:bg-surface-2 utility"
    - "Team-identity outline ring via boxShadow inset 0 0 0 1.5px var(--color-radiant|dire)"
    - "Filled item/loot gold-tint via boxShadow inset 0 0 0 1px var(--color-primary-soft)"
key-files:
  created:
    - .planning/phases/10.4-visual-redesign-and-responsive/10.4-04-SUMMARY.md
  modified:
    - client/src/components/HeroPlayerGrid.tsx
    - client/src/components/PlayerRow.tsx
    - client/src/components/SkeletonPlayerRow.tsx
    - client/src/components/ItemsBlock.tsx
    - client/src/components/CooldownsBlock.tsx
key-decisions:
  - "Pre-existing player-intel tooltip wiring (playerIntelMap prop threading through HeroPlayerGrid->PlayerRow) preserved through the retoken; only the imperative row-hover style mutation was migrated to CSS, the portrait IntelTooltip state handlers stay"
  - "CooldownsBlock team-outlined ult icon required passing player.team into UltSlot (added team prop) — no behavior change"
  - "CooldownsBlock min-h-0 overflow-y-auto (10.2-03 fix) and the sigRef/referenceRef/setInterval client-tick logic preserved verbatim"
requirements: [UI-01, UI-03]
duration: split (Task 1 worktree executor; Task 2 inline orchestrator)
completed: 2026-06-07
---

# Phase 10.4 Plan 04: Match Row-1 Dense Panels Retoken Summary

**Migrated the Match Row-1 dense panels (sketch 004-B) to Tactical Slate tokens with zero color-hex literals: HeroPlayerGrid/PlayerRow/SkeletonPlayerRow (team-ringed portraits, gold mono NW, CSS hover), ItemsBlock (gold-tinted filled slots, CSS hover, min-w-0 slot protection), and CooldownsBlock (steel 15px mono timer, team-outlined ult icon) — UI-03 legibility floors, the pre-existing intel-tooltip wiring, and the CooldownsBlock overflow + client-tick behavior all preserved; 105-test suite green.**

## Performance
- **Tasks:** 2 / 2 completed
- **Files modified:** 5

## Execution note
Task 1 (HeroPlayerGrid/PlayerRow/SkeletonPlayerRow) was completed by the parallel worktree executor and rescued/committed by the orchestrator (`b48454e`) after the executor hit a session permission block before its own commit. Task 2 (ItemsBlock/CooldownsBlock) was completed inline by the orchestrator (`1f4a8ee`) after the same permission environment blocked background-agent writes. All work is committed on master.

## What Was Built

### Task 1 — HeroPlayerGrid + PlayerRow + SkeletonPlayerRow
- Tokens replace all inline hex; CSS row hover (`hover:bg-surface-2`) replaces the imperative `.style.background` handler; team-colored portrait outline rings; `text-radiant`/`text-dire` group labels; gold mono NW; deaths in `var(--color-dire)`. Intel-tooltip wiring preserved (portrait hover state handlers + `playerIntelMap` prop).
- SkeletonPlayerRow shimmer gradient retoned to neutral surface tones.

### Task 2 — ItemsBlock + CooldownsBlock
- ItemsBlock: tokens, CSS hover, gold-tinted filled main slots (`inset 0 0 0 1px var(--color-primary-soft)`), gold neutral-slot border (`var(--color-primary)`), gold mono NW, `min-w-0` so the 6 slots never wrap; imperative hover removed.
- CooldownsBlock: tokens, CSS hover, steel 15px mono timer (`var(--color-accent)`), `s` suffix `text-text-dim`, team-outlined ult icon (added `team` prop to UltSlot), ready label `var(--color-radiant)`; `min-h-0 overflow-y-auto` (10.2-03) and `setInterval` client-tick preserved.

## Verification

- `grep -E "#[0-9a-fA-F]{3,6}"` across all 5 files → **0 matches**.
- PlayerRow: `hover:bg-surface-2` present, no `.style.background`. HeroPlayerGrid: `text-radiant` + `text-dire`.
- ItemsBlock: 0 imperative hover, ≥1 `hover:bg-surface-2`. CooldownsBlock: 0 imperative hover, `min-h-0 overflow-y-auto` present, `var(--color-accent)` present, `setInterval` present.
- `cd client && npx vitest run` → **105 passed (15 files)**, exit 0.

UI-03 phone legibility validated at the Wave 4 UAT checkpoint (Plan 08).

## Deviations from Plan

None to the planned changes. Process deviation: the plan assumed full worktree-executor completion; the permission environment forced Task 2 and the Task 1 commit to be completed inline by the orchestrator (per user decision "Finish inline, no subagents").

## Known Stubs

None — presentational restyle of existing wired data only.

## Self-Check: PASSED

- FOUND: all 5 Row-1 files modified, committed in b48454e (Task 1) and 1f4a8ee (Task 2)
- FOUND: .planning/phases/10.4-visual-redesign-and-responsive/10.4-04-SUMMARY.md
- Commits b48454e and 1f4a8ee present in git log.
