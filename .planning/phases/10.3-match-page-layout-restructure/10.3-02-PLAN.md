---
phase: 10.3-match-page-layout-restructure
plan: 02
type: execute
wave: 2
depends_on:
  - 10.3-01
files_modified:
  - client/src/components/RoshanBlock.tsx
  - client/src/components/CooldownsBlock.tsx
  - client/src/components/BuildingsSection.tsx
  - client/src/components/HistoryGraphs.tsx
autonomous: true
requirements:
  - UAT-LAYOUT-10.3-04
  - UAT-LAYOUT-10.3-05
  - UAT-LAYOUT-10.3-06
  - UAT-LAYOUT-10.3-07
tags:
  - ui
  - layout
  - components
  - phase-10.3
notes: |
  Per-component sanity sweep after 10.3-01 moves four components into wider slots
  (RoshanBlock 320px → ~480px, BuildingsSection full-width → ~700px,
  HistoryGraphs full-width → ~700px, CooldownsBlock 320px → ~480px).
  This plan is CSS-surgical — adjust inner padding, max-width clamps, and
  fontVariantNumeric where the new slot width creates visual weirdness.
  Verification is largely manual; tsc/vitest/build must stay green.
  Any component that already looks fine at the new width gets ZERO edits.
must_haves:
  truths:
    - "RoshanBlock at ~480px width does not look stretched/empty — content is either centered with a max-width clamp or rearranged horizontally to use the extra space."
    - "CooldownsBlock at ~480px width displays its rows comfortably without truncating hero names or icon overlap."
    - "BuildingsSection at ~700px width does not have giant empty whitespace between Radiant/Dire columns — the layout is balanced for the new width."
    - "HistoryGraphs at ~700px width retains readable peak labels, axis labels, and headline values; the SVG viewBox preserveAspectRatio='none' scaling does not distort labels (labels are SVG <text>, also stretched horizontally — verify legibility)."
    - "No component requires breaking changes to its public props or its parent's mount call."
  artifacts:
    - path: "client/src/components/RoshanBlock.tsx"
      provides: "Width-aware layout (centered max-w clamp OR horizontal redistribution)"
    - path: "client/src/components/HistoryGraphs.tsx"
      provides: "Legibility at ~700px width (font sizes / preserveAspectRatio decision)"
  key_links:
    - from: "RoshanBlock"
      to: "Row 2 right slot in MatchPage"
      via: "wider flex-1 container — component must look intentional at this width"
      pattern: "max-w-\\["
    - from: "HistoryGraphs"
      to: "Row 3 right slot in MatchPage"
      via: "narrower flex-1 container — SVG must scale gracefully"
      pattern: "preserveAspectRatio"
---

<objective>
Sanity-sweep the four components whose slot width changed in 10.3-01: RoshanBlock (320px → ~480px), CooldownsBlock (320px → ~480px), BuildingsSection (full → ~700px), HistoryGraphs (full → ~700px). For each, decide whether the current visuals are acceptable at the new width, or whether targeted CSS tweaks (max-width clamps, internal spacing, font sizes, icon sizes) are needed to keep the block looking intentional. CSS-only changes — no behavior changes, no schema changes, no prop changes.

Purpose: ship sketch 002-C without leaving any component visually broken or "stretched empty" in its new slot.

Output: surgical edits to up to four component files. Any component that looks good at the new width gets ZERO edits — this plan deliberately tolerates a no-op outcome.
</objective>

<execution_context>
@D:/MateProjects/projects/dota/dota_stats/.claude/get-shit-done/workflows/execute-plan.md
@D:/MateProjects/projects/dota/dota_stats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/sketches/002-match-page-layout/README.md
@.planning/sketches/002-match-page-layout/002-C-map-roshan-buildings-history-stacked.html
@.planning/phases/10.3-match-page-layout-restructure/10.3-01-SUMMARY.md
@CLAUDE.md
@client/src/components/RoshanBlock.tsx
@client/src/components/CooldownsBlock.tsx
@client/src/components/BuildingsSection.tsx
@client/src/components/HistoryGraphs.tsx
@client/src/pages/MatchPage.tsx

<interfaces>
<!-- Slot widths after 10.3-01 on a ~1920px viewport (page padding p-8 = 32px each side, row gap-8 = 32px between siblings) -->
<!-- Available content row width ≈ 1920 - 64 = 1856px -->
<!-- Row 1 (3 cols, 2 gaps): each slot ≈ (1856 - 64) / 3 ≈ 597px -->
<!-- Row 2 / Row 3 (2 cols, 1 gap):  each slot ≈ (1856 - 32) / 2 ≈ 912px -->

<!-- At ~1440px viewport: Row 1 slot ≈ 437px, Row 2/3 slot ≈ 672px. -->
<!-- Use these as the design target — components should look intentional from ~430px to ~920px. -->

Component current state (before 10.3-02):

- RoshanBlock root: `flex flex-col flex-1` — content was designed for 320px column. At 597-912px wide it will stretch with vast empty space on either side of small loot icons.
- CooldownsBlock root: `flex flex-col flex-1 min-h-0 overflow-y-auto` (10.2-03). Row layout is `flex items-center, minHeight: 44, gap: 8` with hero portrait | ult slot | countdown text. At 597px the rows will have huge trailing whitespace.
- BuildingsSection root: `py-4`, then `<div className="flex gap-12">` with Radiant column, divider, Dire column. At full width (~1500px) it had `gap-12` = 48px between columns. At 912px the gap is still visually OK, but the whole component is left-aligned with empty right space.
- HistoryGraphs panel: `padding: '24px 28px 28px'`, contains two ChartSection divs. SVG uses `viewBox="0 0 1000 200" preserveAspectRatio="none" width="100%" height={200}`. preserveAspectRatio="none" means horizontal scaling stretches text. At 700px wide that's a 0.7x horizontal scale — labels go from 10px nominal to ~7px effective horizontal. Verify legibility.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RoshanBlock width-aware layout</name>
  <files>client/src/components/RoshanBlock.tsx</files>

  <read_first>
    1. Read RoshanBlock.tsx in full.
    2. Run dev server and open a live in-game match. Observe RoshanBlock in its Row 2 right slot at ~600-900px width.
    3. Decide: keep narrow + centered (option A) or redistribute horizontally (option B).
       - Option A (default, recommended): wrap inner content in a centered max-w-[320px] container so the component reads as a narrow card centered in a wider slot. This is the lowest-risk visual choice and preserves 10.2-03 design intent.
       - Option B: rearrange loot rows side-by-side ("Next" left, "Last Drop" right). Higher visual disruption, only pursue if Option A leaves the slot looking sad after a real-match check.
  </read_first>

  <behavior>
    After this task:
    - When RoshanBlock is the only child of a ~600-900px flex slot, it does NOT look like a half-filled empty container.
    - Default approach (Option A): the existing column layout is wrapped in a `max-w-[360px] mx-auto` container so content reads as a centered card.
    - Component still receives `roshan: RoshanState | null` and returns null when null.
    - 1Hz tick behavior, anchor refs, and respawn drift logic are untouched.
  </behavior>

  <action>
**Option A (default — do this unless live-match observation in `read_first` rules it out):**

Change the root JSX from:

```tsx
return (
  <div className="flex flex-col flex-1">
    {roshan.alive ? (...) : (...)}
    {roshan.killCount >= 1 && roshan.lastKillLoot && (...)}
  </div>
)
```

to:

```tsx
return (
  <div className="flex flex-col flex-1">
    <div className="max-w-[360px] mx-auto w-full">
      {roshan.alive ? (...) : (...)}
      {roshan.killCount >= 1 && roshan.lastKillLoot && (...)}
    </div>
  </div>
)
```

That is — wrap the existing children in a single `<div className="max-w-[360px] mx-auto w-full">`. Outer `flex flex-col flex-1` stays so the component still equalizes height with DotaMapView in Row 2.

Do NOT change any of the inner blocks (alive header, respawn timer, loot rows, last-drop section). Do NOT change loot icon sizes.

**Option B (only if Option A clearly fails the manual check):**

Redistribute "Next drop" and "Last drop" into a single horizontal flex inside the wrapper. Defer to a follow-up plan; do not do this inline.
  </action>

  <verify>
    <automated>cd client && npx tsc --noEmit</automated>
    <automated>cd client && npx vitest run -- RoshanBlock</automated>
  </verify>

  <acceptance_criteria>
    - tsc green
    - RoshanBlock test file (if present) still passes; otherwise full vitest run still green
    - `grep -n "max-w-\[360px\] mx-auto w-full" client/src/components/RoshanBlock.tsx` returns one match
    - Manual UAT-LAYOUT-10.3-04 (below) passes
  </acceptance_criteria>

  <done>
    RoshanBlock looks intentional in its Row 2 right slot at both ~600px and ~900px viewports.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CooldownsBlock width sanity check</name>
  <files>client/src/components/CooldownsBlock.tsx</files>

  <read_first>
    1. Read CooldownsBlock.tsx in full.
    2. Observe CooldownsBlock in its Row 1 right slot at ~440-600px width on a live in-game match.
    3. Decide: do the rows look acceptable, or do they need a max-width clamp / additional content (e.g. ult ability name text) to fill the space?
  </read_first>

  <behavior>
    After this task:
    - CooldownsBlock rows fit comfortably in the new ~440-600px slot.
    - If rows look fine as-is: NO CHANGES to this file. Skip task and document "no-op" in the summary.
    - If rows look sparse: wrap inner content in `max-w-[420px] w-full` (no mx-auto — keep left-aligned for readability of the countdown column).
    - Tick behavior (1Hz), sort logic, ready/cooldown rendering unchanged.
  </behavior>

  <action>
**Default action: no-op.** CooldownsBlock at ~440-600px is expected to be acceptable as-is because its rows already have intentional whitespace between icon → ult-slot → countdown.

**Conditional action (only if the live-match check shows the slot is visually unbalanced):**

Change root from:

```tsx
return (
  <div className="flex flex-col flex-1 min-h-0 overflow-y-auto">
    <p className="text-[10px] uppercase tracking-[0.3em] font-bold mb-4" ...>Cooldowns</p>
    <div className="flex flex-col">...</div>
  </div>
)
```

to:

```tsx
return (
  <div className="flex flex-col flex-1 min-h-0 overflow-y-auto">
    <div className="max-w-[420px] w-full">
      <p className="text-[10px] uppercase tracking-[0.3em] font-bold mb-4" ...>Cooldowns</p>
      <div className="flex flex-col">...</div>
    </div>
  </div>
)
```

Document the decision (no-op vs clamp) in the per-task notes in 10.3-02-SUMMARY.md.
  </action>

  <verify>
    <automated>cd client && npx tsc --noEmit</automated>
    <automated>cd client && npx vitest run -- CooldownsBlock</automated>
  </verify>

  <acceptance_criteria>
    - tsc green
    - CooldownsBlock tests still pass
    - Manual UAT-LAYOUT-10.3-05 (below) passes
  </acceptance_criteria>

  <done>
    CooldownsBlock either confirmed no-op-acceptable or wrapped with `max-w-[420px] w-full`. Documented in summary.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: BuildingsSection width sanity check</name>
  <files>client/src/components/BuildingsSection.tsx</files>

  <read_first>
    1. Read BuildingsSection.tsx in full.
    2. Observe in Row 3 left slot at ~672-912px width.
    3. The component is a simple `flex gap-12` of Radiant column / divider / Dire column. At ~900px it will be left-aligned with empty space on the right.
  </read_first>

  <behavior>
    After this task:
    - BuildingsSection reads as a balanced card in its half-width slot.
    - Default: center the inner flex content with `justify-center` on the row, so radiant/dire columns sit in the middle of the slot rather than left-aligned with empty trailing space.
  </behavior>

  <action>
Change:

```tsx
<div className="py-4">
  <p className="text-[10px] uppercase tracking-[0.3em] mb-4" ...>Buildings</p>
  <div className="flex gap-12">
    {/* Radiant ... divider ... Dire */}
  </div>
</div>
```

to:

```tsx
<div className="py-4">
  <p className="text-[10px] uppercase tracking-[0.3em] mb-4" ...>Buildings</p>
  <div className="flex gap-12 justify-center">
    {/* Radiant ... divider ... Dire */}
  </div>
</div>
```

That is, add `justify-center` to the `flex gap-12` row. Title stays left-aligned (it's outside that flex). No other changes.

Do NOT alter the BuildingDot component, the LANES iteration, or the RADIANT_ORDER/DIRE_ORDER constants.
  </action>

  <verify>
    <automated>cd client && npx tsc --noEmit</automated>
    <automated>cd client && npx vitest run -- BuildingsSection</automated>
  </verify>

  <acceptance_criteria>
    - tsc green
    - BuildingsSection tests (if any) still pass; otherwise full vitest still green
    - `grep -n "flex gap-12 justify-center" client/src/components/BuildingsSection.tsx` returns one match
    - Manual UAT-LAYOUT-10.3-06 (below) passes
  </acceptance_criteria>

  <done>
    BuildingsSection content is centered in its 50%-width slot, looks intentional rather than left-orphaned.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: HistoryGraphs legibility at half-width</name>
  <files>client/src/components/HistoryGraphs.tsx</files>

  <read_first>
    1. Read HistoryGraphs.tsx in full.
    2. Observe in Row 3 right slot at ~672-912px on a live in-game match with at least 5 minutes of accumulated history.
    3. The SVG uses `viewBox="0 0 1000 200" preserveAspectRatio="none" width="100%" height={200}`. preserveAspectRatio="none" stretches both axes independently. At ~700px wide that's a 0.7x horizontal squash. SVG `<text>` font-size is set in user units, so glyphs will also squash horizontally and may look "thin".
    4. Decide:
       - Option A (recommended default): switch `preserveAspectRatio="none"` to `preserveAspectRatio="none"` UNCHANGED if the squash is acceptable AND the chart height stays at 200. Verify peak labels are still readable.
       - Option B: keep preserveAspectRatio="none" but bump font sizes (currently 10-12) up by ~2px to compensate for horizontal compression.
       - Option C: switch to `preserveAspectRatio="xMidYMid meet"` so the chart preserves aspect ratio and shrinks proportionally. Would change visual character of the chart — only consider if A and B both fail.
  </read_first>

  <behavior>
    After this task:
    - Peak labels (`+3.4k @ 5:00`), axis labels (`5m`, `10m`), section labels (`GOLD LEAD`, `XP LEAD`), and headline (`Radiant +3.4k`) are all readable at ~700px chart width.
    - The chart still spans the full HistoryGraphs panel — no fixed pixel width regression.
    - 1Hz skeleton tick, peak detection, and Russian section label "Историческая динамика" all preserved.
  </behavior>

  <action>
**Default action: bump SVG text font-size by 2 across the board (Option B).**

In `ChartSection`, change the four SVG `<text>` `fontSize` props:

- Peak label fontSize: 10 → 12 (two occurrences — r-peak and d-peak text elements)
- X-axis label fontSize: 10 → 12 (one occurrence in the ticks.map)

Also bump the two absolute-positioned span fontSizes outside the SVG (these are HTML, NOT affected by SVG scaling — but bumping them keeps visual hierarchy consistent):
- Section label (`{label}` span at top: 6, left: 6) fontSize: 11 → 12
- Headline (`{headlineText}` span at top: 6, right: 6) fontSize: 12 → 13

Do NOT change:
- viewBox dimensions
- preserveAspectRatio (stays "none")
- The HistoryGraphs panel styles
- The Russian skeleton/title text
- ChartSection math (xOf / yOf / peak detection / path construction)

If after observation the labels remain legible at the original sizes, this becomes a no-op and the file is unchanged — document that decision in the summary.
  </action>

  <verify>
    <automated>cd client && npx tsc --noEmit</automated>
    <automated>cd client && npx vitest run -- HistoryGraphs</automated>
    <automated>cd client && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - tsc green
    - HistoryGraphs.test.ts still passes (it asserts presence of `viewBox`, polylines, peak text, etc. — none of those selectors break when fontSize numbers change; if a test asserts an exact fontSize value, update the test to match new size)
    - build green
    - Manual UAT-LAYOUT-10.3-07 (below) passes
  </acceptance_criteria>

  <done>
    HistoryGraphs labels are legible in the half-width slot, charts still convey gold/xp trend at a glance. Decision documented in summary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UI polish only — CSS class and SVG font-size changes inside existing components. No new data, no new event handlers, no new network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.3-03 | (none) | RoshanBlock / CooldownsBlock / BuildingsSection / HistoryGraphs | accept | Pure visual changes; same data, same components, same render shape. |
</threat_model>

<verification>
**Automated (must all pass):**

```bash
cd client && npx tsc --noEmit
cd client && npx vitest run
cd client && npm run build
```

**Manual UAT (RTL-untestable):**

UAT-LAYOUT-10.3-04 — RoshanBlock at half-width
1. Open dev server on a live in-game match.
2. Locate Row 2 right slot. Confirm RoshanBlock content sits in a centered ~360px-wide card, not stretched edge-to-edge.
3. Verify alive state, dead state (respawn countdown), and Last Drop section all render correctly inside the clamp.

UAT-LAYOUT-10.3-05 — CooldownsBlock at one-third width
1. Locate Row 1 right slot. Confirm cooldown rows render with portrait | ult icon | countdown columns and do not feel sparse.
2. If rows feel orphaned in the left half, the conditional `max-w-[420px] w-full` clamp from Task 2 was the right call; if not, the no-op was correct. Either outcome is acceptable.

UAT-LAYOUT-10.3-06 — BuildingsSection centered
1. Locate Row 3 left slot. Confirm Radiant column / divider / Dire column are horizontally centered inside the slot, not left-aligned with empty right space.

UAT-LAYOUT-10.3-07 — HistoryGraphs legibility
1. Locate Row 3 right slot on a match with ≥5 minutes of accumulated history.
2. Read each peak label aloud (e.g. `+3.4k @ 5:00`). Confirm legible.
3. Read each x-axis tick (`5m`, `10m`, ...). Confirm legible.
4. Confirm headline (`Radiant +3.4k` / `Dire +1.8k`) is visible in top-right of each chart section.
5. Confirm Gold chart and XP chart both render — no skeleton stuck at "Накапливаем историю…".

**If any UAT step fails, document the failure in the SUMMARY and propose remediation. Do not silently revert.**
</verification>

<success_criteria>
1. All four components either explicitly no-op or get the surgical change described.
2. tsc / vitest / build all green.
3. Manual UAT-LAYOUT-10.3-04/05/06/07 all confirmed by the developer on a live tournament match.
4. No prop interfaces change. No data flow changes.
5. SUMMARY.md documents per-component decision (changed vs no-op) and rationale.
</success_criteria>

<output>
After completion, create `.planning/phases/10.3-match-page-layout-restructure/10.3-02-SUMMARY.md` summarizing:
- Per-component decision table (component / changed? / rationale)
- Actual diff applied (or "no-op")
- Manual UAT observations with screenshot references if useful
- Commands run + exit codes
- Commit hash(es) — atomic commit per component changed; no commit for no-op components
</output>
