---
phase: 10.1
slug: background-history-sampler
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-05-09
---

# Phase 10.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 1.x (already configured in `server/`) |
| **Config file** | `server/vitest.config.ts` |
| **Quick run command** | `pnpm --filter server test src/services/historySamplerJob.test.ts -- --run` |
| **Full suite command** | `pnpm --filter server test -- --run` |
| **Estimated runtime** | ~5–8 seconds (job tests only); ~30s full server suite |

---

## Sampling Rate

- **After every task commit:** Run quick command above
- **After every plan wave:** Run full server suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

*(Filled by planner — one row per task in 10.1-XX-PLAN.md files. Test type is `unit` for the job module tests; `manual` only where explicitly noted.)*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 10.1-01-T1 | 10.1-01 | 1 | N/A (operational) | — | Module skeleton — no surface | unit | `cd server && npx tsc --noEmit` | ❌ W0 (created in this task) | ⬜ pending |
| 10.1-01-T2 | 10.1-01 | 1 | N/A (operational) | — | RED tests — locks D-04/D-05/D-06/D-07/D-09 contract | unit | `cd server && npx vitest run src/services/historySamplerJob.test.ts` (RED expected) | ❌ W0 (created in this task) | ⬜ pending |
| 10.1-02-T1 | 10.1-02 | 2 | N/A (operational) | — | runOnce: allSettled fan-out + per-match try/catch (D-04, D-06, D-13) | unit | `cd server && npx vitest run src/services/historySamplerJob.test.ts -t "runOnce"` | ✅ exists from Plan 01 | ⬜ pending |
| 10.1-02-T2 | 10.1-02 | 2 | N/A (operational) | — | startSampler env-gate + idempotent timer; stopSampler awaits in-flight (D-03, D-05, D-07) | unit | `cd server && npx vitest run src/services/historySamplerJob.test.ts` (all 5 GREEN) | ✅ exists from Plan 01 | ⬜ pending |
| 10.1-03-T1 | 10.1-03 | 3 | N/A (operational) | — | index.ts wires startSampler + SIGTERM/SIGINT graceful drain (D-01, D-03) | unit | `cd server && npx tsc --noEmit` && `cd server && npx vitest run` | ❌ W0 (mutated in this task) | ⬜ pending |
| 10.1-03-T2 | 10.1-03 | 3 | N/A (operational) | — | Lifecycle smoke: SIGTERM/SIGINT trigger stopSampler + httpServer.close (D-03) | unit | `cd server && npx vitest run src/index.test.ts` | ❌ W0 (created in this task) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `server/src/services/historySamplerJob.test.ts` — RED stubs for D-04 fan-out, D-04/D-06 isolation, D-05 skip-if-running, D-07 env opt-out, D-09 redis-null, SIGTERM cleanup
- [ ] No new fixtures — reuse inline-object game shape pattern from `historySampler.test.ts`
- [ ] No framework install needed — vitest already in `server/package.json`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end "join mid-game sees history from minute 0" | Phase goal | Requires a real live tournament match in flight | After deploy: clear redis `timeseries:{matchId}` for an active match, wait 30s, hit `/api/live/games?matchId=X`, confirm `history` array has entries with `t` predating the request |
| Railway SIGTERM during deploy does not lose in-flight write | D-03 | Requires Railway deploy environment | Trigger a deploy mid-tick; check logs for `'history sampler stopped'` followed by clean process exit (no `'history sample written'` orphans mid-RPUSH) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
