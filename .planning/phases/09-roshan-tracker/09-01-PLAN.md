---
phase: 09-roshan-tracker
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/src/services/roshanState.test.ts
  - shared/roshanLoot.test.ts
  - server/src/routes/live.roshan.test.ts
  - client/src/components/RoshanBlock.test.tsx
autonomous: true
requirements: [ROSH-01, ROSH-02, ROSH-03, ROSH-04]
must_haves:
  truths:
    - "All Wave 0 RED test files exist and run via vitest"
    - "Tests describe expected behaviour and currently fail (RED) — they are NOT skipped"
    - "Test commands match VALIDATION.md per-task verification map"
  artifacts:
    - path: server/src/services/roshanState.test.ts
      provides: "Pure detector + Redis I/O test suite (RED stubs)"
      min_lines: 60
    - path: shared/roshanLoot.test.ts
      provides: "Loot table lookup test suite"
      min_lines: 30
    - path: server/src/routes/live.roshan.test.ts
      provides: "Integration test for /api/live/games match.roshan shape + persistence"
      min_lines: 50
    - path: client/src/components/RoshanBlock.test.tsx
      provides: "Component test for alive/dead/last-drop render + 1Hz tick"
      min_lines: 60
  key_links:
    - from: server/src/services/roshanState.test.ts
      to: server/src/services/roshanState.ts
      via: import
      pattern: "from ['\"]\\./roshanState"
    - from: shared/roshanLoot.test.ts
      to: shared/roshanLoot.ts
      via: import
      pattern: "from ['\"]\\./roshanLoot"
    - from: client/src/components/RoshanBlock.test.tsx
      to: client/src/components/RoshanBlock.tsx
      via: import
      pattern: "from ['\"]\\./RoshanBlock"
---

<objective>
Wave 0: Author all RED test stubs for Phase 9 BEFORE any production code exists.
Every test must FAIL with "module not found" or "expected X but got undefined" — never `skip()` / `todo()`.
Subsequent plans (02-06) flip these tests to GREEN.

Purpose: Lock the behavioural contract for the Roshan tracker as executable specs.
Output: 4 test files (server, shared, server-integration, client component) — all failing.
</objective>

<execution_context>
@D:/MateProjects/projects/dota/dota_stats/.claude/get-shit-done/workflows/execute-plan.md
@D:/MateProjects/projects/dota/dota_stats/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/09-roshan-tracker/09-CONTEXT.md
@.planning/phases/09-roshan-tracker/09-RESEARCH.md
@.planning/phases/09-roshan-tracker/09-VALIDATION.md
@CLAUDE.md

# Existing test analogs (read these first for style + patterns)
@client/src/hooks/useDraftDetail.test.ts
@client/src/utils/heroUltimateMapper.test.ts

<interfaces>
<!-- Contracts the tests assert against. Plans 02-04 implement these. -->

shared/roshanLoot.ts (will export):
```ts
export const ROSHAN_LOOT_PATCH: '7.41'
export const ROSHAN_LOOT: Record<number, readonly number[]>
export function lookupRoshanLoot(killNumber: number): readonly number[]
```

server/src/services/roshanState.ts (will export):
```ts
export interface RoshanState {
  killCount: number
  prevTimer: number
  kills: Array<{ n: number; gameTime: number; timestamp: number }>
}
export function detectRoshanKill(
  prev: RoshanState | null,
  curTimer: number | undefined,
  gameTime: number,
  now: number,
): { state: RoshanState; killed: boolean }
export function readRoshanState(matchId: number): Promise<RoshanState | null>
export function writeRoshanState(matchId: number, state: RoshanState): Promise<void>
```

BFF response shape (added to EnrichedLiveGameSchema):
```ts
match.roshan: {
  killCount: number
  alive: boolean
  respawnIn: number | null
  lastKillLoot: number[] | null
} | null
```

Verified item IDs (from shared/items.json):
- aegis: 117
- roshans_banner: 1804
- cheese: 33
- refresher_shard: 260
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for shared/roshanLoot.ts</name>
  <files>shared/roshanLoot.test.ts</files>
  <read_first>
    - .planning/phases/09-roshan-tracker/09-VALIDATION.md (per-task verification map for ROSH-02)
    - .planning/phases/09-roshan-tracker/09-CONTEXT.md (D-15..D-17 — table contents)
    - shared/itemMapper.ts (analog import style for shared/ tests)
    - client/src/utils/heroUltimateMapper.test.ts (vitest test layout)
  </read_first>
  <behavior>
    - lookupRoshanLoot(0) returns []
    - lookupRoshanLoot(1) returns [117]                         (Aegis)
    - lookupRoshanLoot(2) returns [117, 1804]                    (Aegis + Roshan's Banner)
    - lookupRoshanLoot(3) returns [117, 1804, 33, 260]           (Aegis + Banner + Cheese + Refresher Shard)
    - lookupRoshanLoot(5) returns [117, 1804, 33, 260]           (clamped to 3+)
    - lookupRoshanLoot(99) returns [117, 1804, 33, 260]
    - lookupRoshanLoot(-1) returns []
    - ROSHAN_LOOT_PATCH === '7.41'
    - All item IDs in any returned array are present as ids in shared/items.json (cross-validate)
  </behavior>
  <action>
    Create `shared/roshanLoot.test.ts` using vitest. Import as:
      `import { describe, it, expect } from 'vitest'`
      `import { lookupRoshanLoot, ROSHAN_LOOT_PATCH } from './roshanLoot'`
      `import items from './items.json'`

    Write the 9 test cases listed in <behavior> above using `expect(lookupRoshanLoot(N)).toEqual([...])`.

    For the items.json cross-validation: build a Set of all ids in items.json, then for each kill in [1..5] assert every id in lookupRoshanLoot(kill) exists in the set.

    DO NOT use `it.todo` / `it.skip`. The tests must fail by import failure (module doesn't exist yet).
  </action>
  <verify>
    <automated>cd D:/MateProjects/projects/dota/dota_stats && npx vitest run shared/roshanLoot.test.ts 2>&1 | grep -E "(FAIL|Error|Cannot find module)"</automated>
  </verify>
  <done>
    - File exists with 9 `it(...)` blocks (no skip/todo)
    - Running `npx vitest run shared/roshanLoot.test.ts` exits non-zero with module-not-found OR test-fail output
    - `grep "ROSHAN_LOOT_PATCH" shared/roshanLoot.test.ts` returns at least 2 hits (import + assertion)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests for server/src/services/roshanState.ts</name>
  <files>server/src/services/roshanState.test.ts</files>
  <read_first>
    - .planning/phases/09-roshan-tracker/09-VALIDATION.md (per-task verification map for ROSH-01, ROSH-04)
    - .planning/phases/09-roshan-tracker/09-RESEARCH.md (Pattern 1 — full detector signature + behaviour table)
    - .planning/phases/09-roshan-tracker/09-CONTEXT.md (D-01..D-09)
    - server/src/cache.ts (Redis client init pattern; shows graceful null fallback)
    - client/src/utils/heroUltimateMapper.test.ts (vitest layout)
  </read_first>
  <behavior>
    Pure detector (`detectRoshanKill`):
    - prev=null, cur=undefined → state={killCount:0, prevTimer:0, kills:[]}, killed=false  (no scoreboard yet)
    - prev=null, cur=0       → state={killCount:0, prevTimer:0, kills:[]}, killed=false  (mid-game alive join)
    - prev=null, cur=300     → state={killCount:1, prevTimer:300, kills:[{n:1,...}]}, killed=true (D-04 bootstrap)
    - prev={killCount:1,prevTimer:0,kills:[…]}, cur=420 → killCount:2, killed=true (the 0→>0 transition)
    - prev={killCount:1,prevTimer:300}, cur=295 → killCount:1, killed=false, prevTimer:295 (counting down)
    - prev={killCount:2,prevTimer:5}, cur=0 → killCount:2, killed=false, prevTimer:0 (just respawned)
    - prev={killCount:2,prevTimer:0}, cur=0 → killCount:2, killed=false (still alive, idempotent)
    - prev={killCount:0,prevTimer:0}, cur=undefined → killCount:0, killed=false, state mirrors prev

    Redis I/O (`readRoshanState`, `writeRoshanState`):
    - readRoshanState returns null when no key exists (mock redis.get → null)
    - readRoshanState parses stored JSON correctly
    - readRoshanState returns null on parse error / redis throw (graceful)
    - writeRoshanState calls redis.set with key `roshan:{matchId}`, JSON.stringify(state), 'EX', 21600
    - writeRoshanState swallows redis errors (no throw)

    Match isolation (ROSH-04):
    - Different matchIds use different Redis keys: `roshan:111` vs `roshan:222`
  </behavior>
  <action>
    Create `server/src/services/roshanState.test.ts`. Use vitest.

    Imports:
      `import { describe, it, expect, vi, beforeEach } from 'vitest'`
      `import { detectRoshanKill, readRoshanState, writeRoshanState } from './roshanState'`

    For Redis I/O tests, mock the cache module:
      `vi.mock('../cache.js', () => ({ redis: { get: vi.fn(), set: vi.fn() } }))`
    Then in each Redis test, import the mocked redis: `import { redis } from '../cache.js'`.
    Use `vi.mocked(redis!.get).mockResolvedValue(...)` to control returns.

    Write the 8 detector tests + 5 I/O tests + 1 isolation test (14 tests total) using exact prev/cur values from <behavior>. Use `Date.now()` mocked to a fixed timestamp where needed.

    For the writeRoshanState TTL assertion: assert `redis.set` was called with last two args === `'EX', 21600`.

    DO NOT use skip/todo. Tests must fail by import failure.
  </action>
  <verify>
    <automated>cd D:/MateProjects/projects/dota/dota_stats/server && npx vitest run src/services/roshanState.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|Error)"</automated>
  </verify>
  <done>
    - File exists with 14 `it(...)` blocks
    - `grep -c "it(" server/src/services/roshanState.test.ts` returns >= 14
    - `grep "roshan:\\${" server/src/services/roshanState.test.ts` confirms key-format assertion exists
    - Test run fails (module not found OR assertions fail)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED integration test for /api/live/games match.roshan + RED component test for RoshanBlock</name>
  <files>server/src/routes/live.roshan.test.ts, client/src/components/RoshanBlock.test.tsx</files>
  <read_first>
    - .planning/phases/09-roshan-tracker/09-VALIDATION.md (rows tagged "live route" + "RoshanBlock")
    - server/src/routes/live.ts (current /games handler — lines 22-116, the place that gets a `match.roshan` field)
    - client/src/components/CooldownsBlock.tsx (closest analog: client-tick + render pattern)
    - client/src/utils/heroUltimateMapper.test.ts (vitest setup in client workspace)
    - client/package.json (verify @testing-library/react availability — if missing, add it as part of this task)
  </read_first>
  <behavior>
    Integration (`live.roshan.test.ts`):
    - Mock getLiveLeagueGames to return one game with match_id=999, scoreboard.duration=600, scoreboard.roshan_respawn_timer=0
    - First call to /api/live/games: response.games[0].roshan.killCount === 0 OR null shape (no kill yet, alive)
    - Second simulated call with roshan_respawn_timer=480 (transition 0→480): response.games[0].roshan.killCount === 1, alive===false, respawnIn===480, lastKillLoot===[117]
    - Third call with roshan_respawn_timer=0 again (after respawn): killCount stays 1, alive===true, respawnIn===null
    - Fourth call with roshan_respawn_timer=480 (kill #2): killCount===2, lastKillLoot===[117, 1804]
    - Persistence: between calls, mock the redis client so the value written by call N is read by call N+1
    - Schema parses `roshan_respawn_timer: 480` without throwing (proves valve.ts schema work in plan 03)
    - Different match_id (888) does NOT inherit state from 999 (ROSH-04)

    Component (`RoshanBlock.test.tsx`):
    - Renders nothing when prop `roshan === null`
    - Alive state: roshan={killCount:1, alive:true, respawnIn:null, lastKillLoot:[117]}
        → renders text "ROSHAN #2" (next kill #) AND >=2 <img> tags (next-kill loot icons + last-drop icon)
    - Dead state: roshan={killCount:1, alive:false, respawnIn:300, lastKillLoot:[117]}
        → renders "RESPAWN" label AND mm:ss countdown text matching /^[0-5]?\d:[0-5]\d$/ (initially 5:00)
    - Countdown ticks: with vi.useFakeTimers(), advance 3000ms → countdown text changes from 5:00 to 4:57
    - LAST DROP row: appears when killCount >= 1 (text "LAST DROP")
    - LAST DROP row: hidden when killCount === 0
  </behavior>
  <action>
    **File 1: `server/src/routes/live.roshan.test.ts`**

    Use vitest. Mock services/valveApi.js (`getLiveLeagueGames`), openDotaApi.js (`getLeagueName`), and the redis client from cache.js (in-memory Map: get/set proxy backed by a single Map<string,string> so writes are visible to subsequent reads).

    Build a Hono test app with `liveRoutes` mounted under `/api/live`. Use `app.request('/api/live/games')` to invoke. Parse response JSON.

    Write 6 test cases as listed in <behavior>. Between calls, do NOT clear the in-memory redis Map (that's the persistence test). Reset only between separate `describe` blocks via `beforeEach(() => map.clear())`.

    **File 2: `client/src/components/RoshanBlock.test.tsx`**

    Imports:
      `import { render, screen, act } from '@testing-library/react'`
      `import { describe, it, expect, vi, afterEach } from 'vitest'`
      `import RoshanBlock from './RoshanBlock'`

    If @testing-library/react is missing from client/package.json (verify with `grep '@testing-library/react' client/package.json`):
      run `cd client && npm install --save-dev @testing-library/react @testing-library/jest-dom jsdom`
      AND ensure `client/vitest.config.ts` has `test: { environment: 'jsdom' }` — add it if missing (verify by reading the file first).

    Write 6 component tests as listed in <behavior>. For tick test, use `vi.useFakeTimers()` then `act(() => { vi.advanceTimersByTime(3000) })`.

    DO NOT use skip/todo.
  </action>
  <verify>
    <automated>cd D:/MateProjects/projects/dota/dota_stats/server && npx vitest run src/routes/live.roshan.test.ts 2>&1 | grep -E "(FAIL|Cannot find module)" && cd ../client && npx vitest run src/components/RoshanBlock.test.tsx 2>&1 | grep -E "(FAIL|Cannot find module)"</automated>
  </verify>
  <done>
    - Both files exist
    - `grep -c "it(" server/src/routes/live.roshan.test.ts` returns >= 6
    - `grep -c "it(" client/src/components/RoshanBlock.test.tsx` returns >= 6
    - Both vitest runs fail (module not found is acceptable — that's the RED state we want)
    - If @testing-library/react was added, `grep '@testing-library/react' client/package.json` returns a hit, and `grep "environment: 'jsdom'" client/vitest.config.ts` returns a hit
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `cd D:/MateProjects/projects/dota/dota_stats && npx vitest run shared/roshanLoot.test.ts` → FAIL (RED)
- `cd D:/MateProjects/projects/dota/dota_stats/server && npx vitest run src/services/roshanState.test.ts src/routes/live.roshan.test.ts` → FAIL (RED)
- `cd D:/MateProjects/projects/dota/dota_stats/client && npx vitest run src/components/RoshanBlock.test.tsx` → FAIL (RED)
- No `it.skip` or `it.todo` anywhere: `grep -rE "it\\.(skip|todo)" shared/roshanLoot.test.ts server/src/services/roshanState.test.ts server/src/routes/live.roshan.test.ts client/src/components/RoshanBlock.test.tsx` returns nothing
</verification>

<success_criteria>
- 4 test files exist, all currently failing
- Test count totals: 9 (loot) + 14 (detector+I/O) + 6 (integration) + 6 (component) = 35 RED tests
- Frontmatter `wave_0_complete` in 09-VALIDATION.md can be flipped to true after Plan 06 closes
</success_criteria>

<output>
Create `.planning/phases/09-roshan-tracker/09-01-SUMMARY.md` listing each test file with the count of tests it contains and the GREEN target plan number (02 / 04 / 05).
</output>
